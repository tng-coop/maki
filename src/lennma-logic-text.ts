// This file is auto-generated. Do not edit directly.
export const lennmaLogicLisp = "\n;;;; File: package.lisp\n\n(in-package :cl-user)\n;;;; package\n(defpackage :lennma-math\n  (:use :cl)\n  )\n\n\n;;;; File: tidtid.lisp\n(in-package :lennma-math)\n\n;;;; Space Theory\n\n(defun cartesian-2 (a b)\n  ;; cartesian product of two list. list a .times list b\n  (let ((result nil)\n        (tail nil))\n    (dolist (a_n a)\n      (dolist (b_n b)\n        (let ((cell (cons (cons a_n b_n) nil)))\n          (if (null result)\n              (setq result cell)\n              (setf (cdr tail) cell))\n          (setq tail cell))))\n    result))\n\n(defun cartesian (lists)\n  ;; cartesian products of lists.\n  (reduce #'(lambda (ls l_n) (cartesian-2 l_n ls))\n          (cons '(()) (reverse lists))))\n\n(defun cartesian-self (a num)\n  \".otimes_i^num A\"\n  (cartesian\n         (make-list num :initial-element a)))\n\n;; tests\n;; (cartesian-2 '(1 2 3) '(a b c))\n;; (cartesian '((1 2 3) (a c) (u v)))\n;; (cartesian-self '(a b c) 3)\n\n\n;;;; \n\n(defparameter +fail+ '+fali+)\n(defparameter .true '.true)\n(defparameter .false '.false)\n\n(defparameter .b_0 '.b_0)\n(defparameter .succ '.succ)\n\n\n\n;;;;\n\n\n\n\n\n\n(defun _infocus-.in (.s .t)\n  (cond ((and (_type-x-term? .s)\n              (_type-x-term? .t)\n              (= (1+ (_term-rank .s)) (_term-rank .t))\n              (in? .s .t))\n         .true)\n        (t .false)))\n\n\n(defun _atomic-formula? (symbols)\n  \n  )\n         \n;; infocus <-> outfocus\n\n\n;; substitute logic-expression.\n;; name space is sepalated by types and steps:\n;; subst-logic-expression >=> subst-term-expression >=> subst-value-expression\n\n(defun subst-logic-expr (expr substs)\n  \"substitute logic expression. used for all logic expressions.\"\n  \n  )\n\n(defun subst-term-expr ()\n  \"substitute term expression. used for substitute term.\"\n  )\n\n(defun subst-value-expr ()\n  \"substitute value expression. used for substitute specific value.\"\n  )\n\n\n;; (defun subst (A-logic-formula x-variable t-value)\n;;   (labels ((aux (formula-current tails)\n;;              (cond ((null formula-current) tails)\n;;                    ((or (eql (A-logic-formula\n             \n;;   (cond ((eql (term-rank variable) (term-ranm value))\n;;          )\n;;         )\n\n  \n;;;; 凡例\n;;;; _foobar :: 直感的な数学に属する操作的な定義\n;;;; .foobar :: 形式的な数学に属する操作的かつ形式的な定義\n\n  (defun make-expr (&rest args)\n    (apply #'list args))\n\n(defun _pairlis (keys datas &optional alist)\n  \"clone of pairlis\"\n  (append (mapcar #'cons keys datas) alist))\n\n\n(defun subst2 (expr substs)\n  (labels ((aux (rest substs_space)\n             (let ((func (car rest))\n                   (arg-1 (cadr rest))\n                   (arg-body (cddr rest)))\n               (cond ((null func) nil)\n                     ;; name collision at .forall/.exists\n                     ((and (eql func '.forall))\n                      (make-expr func\n                                 (identity arg-1)\n                                 (aux (rename arg-body substs)\n                                      (identity substs_space))))\n                     ((and (eql func '.exists)\n                          (member '.exists arg-1))\n                      (cons func arg-1))\n                     ;;\n                     (t nil)))))\n\n    ))\n\n\n(defun _lambda_test1 (args body vars)\n  (labels ((aux (_body _vars)\n             (if (null _body)\n                 '()\n                 (let ((ed_subst (assoc (car _body) _vars)))\n                   (cond (ed_subst\n                          (cons (cdr ed_subst)\n                                (aux (cdr _body)\n                                     _vars)))\n                         (t\n                          (cons (car _body)\n                                (aux (cdr _body)\n                                     _vars))))))))\n    (aux body (_pairlis args vars nil))))\n\n\n;;;;\n\n(defun unify ()\n  )\n\n(defun apply-theorem (theorem-func theorem-args-star)\n\n  )\n\n\n\n\n\n;;;;\n(defmacro _def-axiom (name &body body)\n  name body\n  'dummy-implemetion ;; todo\n  nil)\n\n;;; peano axioms\n(_def-axiom A_I_.b_0\n  (amb ()\n       (.in .b_0 .natural-number)))\n\n(_def-axiom A_I_.succ\n  (amb ()\n       (.forall (.xi_1)\n           (.to (.in .xi_1 .natural-number)\n               (.in (.succ .xi_1) .natural-number)))))\n\n(_def-axiom A_I_1\n  (amb ()\n       (.forall (.xi_1)\n           (.to (.ni .natural-number .xi_1)\n               (.neg (.= (.succ .xi_1) .b_0))))))\n\n(_def-axiom A_I_2\n  (amb ()\n       (.forall (.xi_1 .eta_1)\n           (.to (.ni .natural-number .xi_1 .eta_1)\n               (.to (.= (.succ .xi_1) (.succ .eta_1))\n                   (.= .xi_1 .eta_1))))))\n\n(_def-axiom A_I_3\n  (amb ()\n       (.forall (.xi_2)\n           (.to (.ni .natural-number-set .xi_2)\n               (.to (.and (.in .b_0 .xi_2)\n                       (.forall (.xi_1)\n                           (.to (.ni .natural-number .xi_1)\n                               (.to (.ni .xi_2 .xi_1)\n                                   (.in (.succ .xi_1) .xi_2)))))\n                   (.forall (.xi_1)\n                       (.to (.ni .natural-number .xi_1)\n                           (.in .xi_1 .xi_2))))))))\n\n;;; propositional logic axioms\n\n(_def-axiom A_II_1\n  (amb ()\n       (.forall (A B)\n           (.to (.ni .logic-formula A B)\n               (.to A (.to B A))))))\n\n(_def-axiom A_II_2\n  (amb ()\n       (.forall (A B C)\n           (.to (.ni .logic-formula A B C)\n               (.to (.to A (.to B C))\n                   (.to (.to A B) (.to A C)))))))\n\n(_def-axiom A_II_3\n  (amb ()\n       (.forall (A B)\n           (.to (.ni .logic-formula A B)\n               (.to (.to (.neg B) (.neg A))\n                   (.to B A))))))\n\n\n;;; predicate logic axioms\n\n(defun always-ok (&rest args)\n  (declare (ignore args))\n  ;; 't\n  'ok)\n\n(defun lambda-intutionism-filter (pattern-vars match-pattern filter-function axiom-when-not-nil)\n  )\n\n(defun lambda-rebind (pattern-vars match-pattern filter-function axiom-when-not-nil)\n  )\n\n;; A_III_1\n;; (.to (.forall (x) (F x)) (F t))\n\n(_def-axiom A_III_1\n  (lambda-rebind\n   (?x ?t)\n   (.forall (?F)\n       (.to (.ni .function-1arg ?F)\n           (.to (.forall (?x) (?F ?x))\n               (?F ?t))))\n   #'always-ok\n   (amb ((at_t (generator-domain ?x))) ;; at_t is domain of definition similar to ?x\n        (.forall (F)\n            (.to (.ni .function-1arg F)\n                (.to (.forall (x) (F x))\n                    (F at_t)))))))\n\n\n;; A_III_2\n;; if x is not appeared at A's variables,\n;; (.forall (x) (A .to f(x))) .to (A .to (.forall (x) (f(x))))\n\n(_def-axiom A_III_2\n  (lambda-intuitionism-filter\n   (?x ?A)\n   (.forall (?A ?f)\n       (.to (.and (.ni .logic-formula ?A)\n               (.ni .function-1arg ?f))\n           (.to (.forall (?x) (.to ?A (?f ?x)))\n               (.to ?A (.forall (?x) (?f ?x))))))\n   #'(lambda (x1 A1)\n       (eval-logic-immediate (.neg (.ni x1 (.free-variables A1)))))\n   (amb ()\n        (.forall (A f)\n            (.to (.and (.ni .logic-formula A)\n                    (.ni .function-1arg f))\n                (.to (.forall (x) (.to A (f x)))\n                    (.to A (.forall (x) (f x)))))))))\n\n;; ToDo\n;; 推論規則\n\n;; 推論規則1\n;; (_def-axiom ...)\n\n;; 推論規則2\n;; (_def-axiom ...)\n\n\n;; notes\n;;\n;; # 命題論理の世界を探索して、その全経路をハウスドルフな地図として残す評価機械、\n;; # 或いは、式を式の儘に性質を保存しながら性質を吟味しようとする思考形態を部分的ながら再現するプログラム。\n;;\n;; ambに生成元の定義域が規程されていないときは、\n;; ambの値はシンボルであるが、\n;; ambに生成元の定義域が規定されているときにも、ambがambの間は評価前の式であって具体的な対象式のシンボルを必要としない。 \n;; 値・式・命題・証明文というデータ構造へとプログラムがとるべき評価戦略は、\n;; 数値処理プログラムに対する数式処理プログラムの評価戦略と全くの同質である。\n;;\n;; 通常のevalは優先順位が最高位置にある式を評価し続けることによって\n;; 評価の進行過程や評価の評価結果などを1に纏めている一本線状の評価戦略だが、\n;; ambは評価しうる全ての式を並列に評価するという可能性を全列挙する計算で樹状の評価戦略である。\n;;\n;; しかし、無限計算機が遭って単純なambによる全列挙が出来たとしても、\n;; 意味を失う様では、データを完全に再現できたとしても、本質的な意味を失う。\n;; 自己言及的な大系とその脈動にとって、本当に意味があることとは、\n;; 本質的に重要な解釈は保存されるという性質が、保存されることである。\n;; その解釈とは、人間の世界に於ける定理証明支援系の発達段階の初期に於いては、\n;; 式として表現され、かつ、プログラムに依ってデータへと酸化されるとは限らない通常は式は式の状態ままに、\n;; 式についての性質解釈群を通して、解釈され、あるいは通常は式の儘に、吟味されるものとなる。\n\n;;;;\n\n(defstruct (asms (:conc-name asms-)\n                     (:constructor make-asms))\n  \"Axiom/aSsumption/leMma/conSequence\"\n  ;; \"AXIom/ASSumption/LEMma/conSEqueNce\"\n  \n  ;;\n  ;;\n  (number -1)\n  (variables '())\n  (logic-expression '())\n  ;;\n  (le-from '())\n  (name \"\")\n  (comment \"\"))\n\n(defun asms-args (asms)\n  (asms-variables asms)\n  ;; todo:and types\n  nil\n)\n\n;;;; \n\n(defun _apply-amb (logic variables)\n  \n\n  \n  )\n\n(defun _eval-amb (asmses)\n  (mapcar\n   #'(lambda (asms)\n       (let ((arg-types (asms-args asms)))\n         (length arg-types)))\n   asmses)\n  )\n\n\n\n;;;; exam\n\n(defun exam-proofable ()\n  ())\n\n\n;;;; File: utils.lisp\n(in-package :lennma-math)\n\n(defun subsetp (xs ys &key (test #'equal))\n  (every #'(lambda (x) (member x ys :test test)) xs))\n\n(defun safe-append (&rest lists)\n  (let ((result nil)\n        (tail nil))\n    (do ((curr lists (cdr curr)))\n        ((null curr))\n      (let ((lst (car curr)))\n        (cond ((null (cdr curr))\n               (if (null result)\n                   (setq result lst)\n                   (setf (cdr tail) lst)))\n              (t\n               (dolist (x lst)\n                 (let ((cell (cons x nil)))\n                   (if (null result)\n                       (setq result cell)\n                       (setf (cdr tail) cell))\n                   (setq tail cell)))))))\n    result))\n\n;;;; Utility Procedures\n\n(defun keyref1 (key plist)\n  (assert (keywordp key) (key)\n          \"KEY must be a keyword, got ~S\" key)\n  (labels ((aux (rest)\n             (cond ((null rest)             nil)\n                   ((eql key (car rest))    (cadr rest))\n                   (t                       (aux (cddr rest))))))\n    (aux plist)))\n\n\n(defun mapcar-remove-nil-flatten (function list &rest more-lists)\n  \"(flatten (remove nil (mapcar function list &more-lists)))\"\n  (apply #'append (remove nil (apply #'mapcar function list more-lists))))\n\n(defun mapcar-remove-nil (function list &rest more-lists)\n  \"(remove nil (mapcar function list &more-lists))\"\n  (remove nil (apply #'mapcar function list more-lists)))\n\n\n;;;; File: lisp1.lisp\n(in-package :lennma-math)\n\n;; References:\n;; [JM1960] John McCarthy: Recursive Functions of Symbolic Expressions and Their Computation by Machine, Part I.\n;; - [recursive.dvi](https://www-formal.stanford.edu/jmc/recursive.pdf). (April 1960).\n;;\n;; [PG_jmc] Paul Graham: McCarthy's Original Lisp\n;; - or [jmc.lisp](https://sep.turbifycdn.com/ty/cdn/paulgraham/jmc.lisp)\n;; - or [The Roots of Lisp](https://paulgraham.com/rootsoflisp.html)\n\n\n;;;;;; # chapter 3. Recrsive Functions of Symbolic Expressions. from [JM1960] John McCarthy (1960)\n\n\n\n\n;;;; \\S a. A class of Symbolic Expressions\n\n(defvar _ty_atom_  '_ty_atom_ )\n(defvar _ty_cons_  '_ty_cons_ )\n(defvar _ty_error_ '_ty_error_)\n\n(deftype _s-tag ()\n  '(member _ty_atom_ _ty_cons_ _ty_error_))\n\n(defstruct (_S (:constructor %_make-_S) (:conc-name %_S-))\n  (_ty _ty_error_ :type _s-tag) ;; type\n  (_vc nil) ;; value of car / value of atom\n  (_vd nil) ;; value of cdr / message of error\n  )\n\n(defun make-_S-atom (a)\n  (%_make-_S :_ty _ty_atom_  :_vc a   :_vd nil))\n(defun make-_S-cons (se1 se2)\n  (%_make-_S :_ty _ty_cons_  :_vc se1 :_vd se2))\n(defun make-_S-error (&optional message)\n  (%_make-_S :_ty _ty_error_ :_vc nil :_vd message))\n\n\n(defvar _true  (make-_s-atom '_true))\n(defvar _false (make-_s-atom '_false))\n(defvar _nil   (make-_s-atom '_nil))\n(defvar _fail  (make-_s-atom '_fail))\n(defvar _error (make-_s-error '_error))\n\n\n(defun cl{_S-_atom?}  (_s) (if (eq (%_s-_ty _s) _ty_atom_) t nil))\n(defun cl{_S-_cons?}  (_s) (if (eq (%_s-_ty _s) _ty_cons_) t nil))\n(defun cl{_S-_error?} (_s) (if (eq (%_s-_ty _s) _ty_error_) t nil))\n(defun cl{_S-_null?}  (_s) (if (eq _s _nil) t nil))\n\n\n(defun S->_S (args)\n  (labels ((aux (vs)\n             (cond ((null vs) _nil)\n                   ((atom vs) (make-_s-atom vs))\n                   (t         (make-_s-cons (aux (car vs))\n                                            (aux (cdr vs)))))))\n    (aux args)))\n\n\n(defun _S->S (args)\n  (labels ((aux (vs)\n             (unless (_s-p vs) (error \"unexpected _S: ~A\" vs))\n             (cond ((cl{_s-_null?} vs)  nil)\n                   ((cl{_s-_atom?} vs)  (%_s-_vc vs))\n                   ((cl{_s-_cons?} vs)  (cons (aux (%_s-_vc vs))\n                                              (aux (%_s-_vd vs))))\n                   ((cl{_s-_error?} vs) _error)\n                   (t                   (error \"unexpected _S given: ~A\" vs)))))\n    (aux args)))\n\n;;;;\n\n(defun _error-message (func-name arg-list &optional (message \"\"))\n  ;;(declare (ignore func-name arg-list message))\n  (terpri)\n  (cond ((not (every #'_S-p arg-list))\n         (format t \"_error found : not _S object given at:~% (~{~A~^ ~})~%\"\n                 (cons func-name arg-list)))\n        (t\n         (format t \"_error found : ~A at:~%    [_S->S](~{~A~^ ~})~%\"\n                 message (cons func-name (mapcar #'_S->S arg-list)))))\n  _error)\n\n(defmacro def__lisp1_pred (name lambda-list &body body)\n  `(defun ,name ,lambda-list\n     ;; type error check for _S\n     ,@(mapcar (lambda (arg)\n                 `(unless (_s-p ,arg)\n                    (error \"unexpected _S: ~A\" ,arg)))\n               lambda-list)\n     ;; body\n     ,@body))\n\n;;;;\n\n\n;;;; \\S zero. Initial Defines\n\n(def__lisp1_pred _not (exp)\n  (cond ((eq exp _true)  _false)\n        ((eq exp _false) _true )\n        (t _error)))\n\n(defmacro _if (test then else)\n  `(cond ((cl{_s-_error?} ,test) _error)\n         ((eq ,test _true)  ,then)\n         ((eq ,test _false) ,else)\n         (t _error)))\n\n(def__lisp1_pred _and (x y)\n  (_if x (_if y _true _false) _false))\n\n(def__lisp1_pred _or (x y)\n  (_if x _true (_if y _true _false)))\n\n\n;;;; \\S c. The Elementary S-functions and Predicates.\n\n(def__lisp1_pred _atom (x)\n  (cond ((eq (%_s-_ty x) _ty_atom_)\n         _true)\n        ((eq (%_s-_ty x) _ty_cons_)\n         _false)\n        ((eq (%_s-_ty x) _ty_error_)\n         _error)\n        (t (error \"~A is not _atom\" x)\n           nil ;; _error\n           )))\n\n(def__lisp1_pred _eq (x y)\n  (_if (_and (_atom x) (_atom y))\n       (if (eq (%_s-_vc x) (%_s-_vc y))\n           _true _false)\n       (_error-message '_eq (list x y) \"_eq is defined if and only if both x and y are atomic\")))\n\n(def__lisp1_pred _car (x)\n  (if (cl{_s-_cons?} x)\n      (%_s-_vc x)\n      _error))\n\n(def__lisp1_pred _cdr (x)\n  (if (cl{_s-_cons?} x)\n      (%_s-_vd x)\n      _error))\n\n(def__lisp1_pred _cons (x y)\n  (if (or (eq x _error) (eq y _error))\n      _error\n      (make-_s-cons x y)))\n\n\n(def__lisp1_pred _list2 (x y)\n  (_cons x (_cons y _nil)))\n\n(def__lisp1_pred _caar   (x)   (_car (_car x)))\n(def__lisp1_pred _cadr   (x)   (_car (_cdr x)))\n(def__lisp1_pred _cadar  (x)   (_car (_cdr (_car x))))\n(def__lisp1_pred _caddr  (x)   (_car (_cdr (_cdr x))))\n(def__lisp1_pred _caddar (x)   (_car (_cdr (_cdr (_car x)))))\n\n\n;;;; \\S d. Recursive S-functions\n\n(def__lisp1_pred _ff (x)\n  (labels ((_aux (u)\n             (_if (_atom u)\n                  u\n                  (_ff (_car u)))))\n    (_aux x)))\n\n\n(def__lisp1_pred _subst (x y z)\n  (labels ((_aux (u)\n             (_if (_atom u)\n                  (_if (_eq u y)\n                       x u)\n                  (_cons (_aux (_car u))\n                         (_aux (_cdr u))))))\n    (_if (_not (_atom y))\n         (_error-message '_subst (list x y z) \"y is not symbol\")\n         (_aux z))))\n\n\n(def__lisp1_pred _equal (x y)\n  (_or (_if (_and (_atom x) (_atom y)) (_eq x y) _false)\n       (_if (_and (_not (_atom x)) (_not (_atom y)))\n            (_and (_equal (_car x) (_car y))\n                  (_equal (_cdr x) (_cdr y)))\n            _false)))\n\n\n(def__lisp1_pred _null (x)\n  (_if (_atom x)\n       (_eq x _nil)\n       _false))\n\n\n(def__lisp1_pred _append (x y)\n  (labels ((_aux (u)\n             (_if (_null u)\n                  y\n                  (_cons (_car u) (_aux (_cdr u))))))\n    (_if (_and (_atom x) (_not (_null x)))\n         (_error-message '_append (list x y) \"argument is not list (arg1)\")\n         (_if (_and (_atom y) (_not (_null y)))\n              (_error-message '_append (list x y) \"argument is not list (arg2)\")\n              (_aux x)))))\n\n(def__lisp1_pred _among (x y)\n  (labels ((_aux (v)\n             (format t \"aux: ~A, ~A~%\" (_s->s x) (_s->s v))\n             (_if (_null v)\n                  _false\n                  (_or (_equal x (_car v))\n                       (_aux (_cdr v))))))\n    (_if (_not (_atom y))\n         (_aux y)\n         (_error-message '_among (list x y) \"2nd arg is not list\"))))\n\n(def__lisp1_pred _pair (x y)\n  (_if (_and (_null x) (_null y))\n       _nil\n       (_if (_and (_not (_atom x)) (_not (_atom y)))\n            (_cons (_list2 (_car x) (_car y))\n                   (_pair (_cdr x) (_cdr y)))\n            (_error-message '_pair (list x y) \"length is not same, or some argument is atom.\"))))\n\n(def__lisp1_pred _pairlist1-p (l)\n  \"detect _alist: ((u_1 v_1) ... (u_n v_n)) where u_i must be atom.\"\n  (labels ((_aux (ls)\n             ;; (print (_s->s ls))\n             (_if (_null ls)\n                 _true\n                 (_if (_and (_not (_atom ls))                     ;; is ls list?\n                            (_and (_atom (_car (_car ls)))        ;; is u_i atom?\n                                  (_null (_cdr (_cdr (_car ls)))) ;; is ls form (u_i v_i)? == (A* . (B* . NIL)) ?\n                                  ))\n                      (_aux (_cdr ls))\n                      _false))))\n  (_if (_not (_atom l)) ;; list? l\n       (_aux l)\n       (_if (_null l)\n            _true\n            _false))))\n\n(def__lisp1_pred _assoc (x y)\n  ;;\n  (labels ((_aux (v)\n             (_if (_null v)\n                  ;;(progn (_error-message '_assoc (list x y) \"the item is not found in alist\")\n                  ;;_fail)\n                  _fail\n                  (_if (_eq (_caar v) x)\n                       (_cadar v)\n                       (_aux (_cdr v))))))\n    ;; todo: detect pairlist1-p\n    (_if (_not (_atom x))\n         (_error-message '_assoc (list x y) \"1st must be atom\")\n         (_if (_and (_atom y) (_not (_null y)))\n              (_error-message '_assoc (list x y) \"2nd arg is not list\")\n              (_aux y) ;; memo (not (atom? z)) means (consed? z)\n              ))))\n\n\n\n(def__lisp1_pred _sub2 (x z)\n  (labels ((_aux (v)\n             (_if (_null v)\n                  z\n                  (_if (_eq (_caar v) z)\n                       (_cadar v)\n                       (_aux (_cdr v))))))\n    (_aux x)))\n\n(def__lisp1_pred _sublis (x y)\n  (labels ((_aux (v)\n             (_if (_atom v)\n                  (_sub2 x v)\n                  (_cons (_aux (_car v))\n                         (_aux (_cdr v))))))\n    ;; todo: detect pairlist-p\n    (_aux y)))\n\n\n;;;; \\S e. Representation of S-Function by S-Expressions.\n;;;; pass\n\n;;;; \\S f. The Universal S-function apply.\n\n(defvar _quote (make-_s-atom 'quote))\n\n(def__lisp1_pred _appq (m)\n  (_if (_null m)\n       _nil\n       (_cons (_list2 _quote\n                      (_car m))\n              (_appq (_cdr m)))))\n\n(def__lisp1_pred _apply (f args)\n  (_eval-before (_cons f (_appq args))\n                ;;(s->_s nil)))\n                _nil))\n\n(defparameter *_eval-nest* 0)\n(defun _indent-text (num-nest)\n  ;; format ~v times of spaces : \" \" by <~>\n  (format nil \"~v<~>\" num-nest))\n(defparameter *_eval-format-trace* nil)\n\n;; (defvar _paradox '_paradox)\n;; (defvar _unresolved '_unresolved)\n\n(defvar _*global-alists*\n  (s->_s '(;;(_paradox _paradox)\n           (_nil _nil)\n           (_false _false)\n           (_true _true)\n           ;;(_unresolved _unresolved)\n           ;;(_fail _fail)\n           (_error _error))))\n\n\n(def__lisp1_pred _eval-before (e a)\n  (_if (_not (_pairlist1-p a))\n       (_error-message '_eval-before (list e a) \"2nd list is not alist : ((u_1 v_1) ... (u_n v_n)) where u_i must be atom.\")\n       (progn\n         (setq *eval-nest* 0)\n         (_eval e a))))\n\n(defun _eval (e a)\n  ;; e: expressions, a: alists\n  (when *_eval-format-trace*\n    (format t \"~AC_N~A: (_eval [@S]~A~%~A [@S]~A)~%\"\n            (_indent-text *_eval-nest*)        *_eval-nest* (_s->s e)\n            (_indent-text (+ *_eval-nest* 12)) (_s->s a)))\n  (incf *_eval-nest* 1)\n  ((lambda (result_1)\n     (decf *_eval-nest* 1)\n     ; (format t \"RES:: ~A~%\" result_1)\n     (when (or *_eval-format-trace*\n               (eq result_1 _error))\n       (format t \"~AV_N~A: [@S]~A~%\"\n               (_indent-text *_eval-nest*)        *_eval-nest* (_s->s result_1)))\n     (cond ((eq result_1 _error)\n            (format t \"~AE_N~A: (_eval [@S]~A~%\"\n                    (_indent-text *_eval-nest*)        *_eval-nest* (_s->s e))\n            (format t \"~A [@S]~A)~%\"\n                    (_indent-text (+ *_eval-nest* 12)) (_s->s a))\n            (format t \"~A is _error~%\"\n                    (_indent-text (+ *_eval-nest* 12)))\n            _error)\n           (t\n            result_1)))\n   (cond ((eq (_atom e) _true)\n          (let ((v_a (_assoc e a)))\n            (if (not (eq v_a _fail))\n                v_a\n                (let ((v_g (_assoc e _*global-alists*)))\n                  (if (not (eq v_g _fail))\n                      v_g\n                      (progn (_error-message e () \"unknown symbol\")\n                             _error))))))\n         ((eq (_atom (_car e)) _true)\n          (let ((carsym (_s->s (_car e))))\n            (cond ((eq carsym 'quote) (_cadr e))\n                  ((eq carsym 'atom)  (_atom (_eval (_cadr e) a)))\n                  ((eq carsym 'eq)    (_eq (_eval (_cadr e) a) (_eval (_caddr e) a)))\n                  ((eq carsym 'cond)  (_evcon (_cdr e) a))\n                  ((eq carsym 'car)   (_car (_eval (_cadr e) a)))\n                  ((eq carsym 'cdr)   (_cdr (_eval (_cadr e) a)))\n                  ((eq carsym 'cons)  (_cons (_eval (_cadr e) a) (_eval (_caddr e) a)))\n                  (t                  (_eval (_cons (_assoc (_car e) a)\n                                                    (_evlis (_cdr e) a))\n                                             a)))))\n         ((eq (_s->s (_caar e)) 'label)\n          (_eval (_cons (_caddar e) (_cdr e))\n                 (_cons (_list2 (_cadar e) (_car e))\n                        a)))\n         ((eq (_s->s (_caar e)) 'lambda)\n          (_eval (_caddar e)\n                 (_append (_pair (_cadar e)\n                                 (_evlis (_cdr e) a))\n                          a)))\n         (t _error))))\n\n(def__lisp1_pred _evcon (c a)\n  (_if (_null c)\n       (progn (_error-message '_evcon (list c) \"cond did not match any cases\")\n         _error)\n       (let ((v (_eval (_caar c) a)))\n         (_if (_eq v _true)\n              (_eval (_cadar c) a)\n              (_evcon (_cdr c) a)\n              ;; otherwise: _error\n              ))))\n\n(def__lisp1_pred _evlis (m a)\n  (_if (_null m)\n       _nil\n       (_cons (_eval (_car m) a)\n              (_evlis (_cdr m) a))))\n\n\n\n;;;; \\S g. Functions with Functions as Arguments.\n\n\n;;;;;; # REPL for _eval\n\n(defun l1-repl ()\n  (labels\n      ((aux (a)\n         (format t \"_L1> \")\n         (let* ((input (read))\n                (ret (_eval input a)))\n           (cond ((equal input '(exit))\n                  (format t \"bye.~%\") nil)\n                 (t\n                  (format t \"~A~%\" ret)\n                  (aux a))))))\n    (aux _nil)))\n\n\n;;;;;; # Examples\n;; from [JM1960] John McCarthy (1960)\n\n(defun _examples-_eval ()\n  ;; when eval\n  (_apply (s->_s '(label ff (lambda (x) (cond ((atom x) x)\n                                              ((quote _true)\n                                               (ff (car x)))))))\n          (s->_s '(('a . 'b))))\n  ;; => A\n\n  ;; (_s->s (_eval-before (s->_s '((lambda (x y) (cons (car x) y)) '(a b) '(c d))) _nil))\n  ;; => (A C D)\n\n  )\n\n\n;; (_eval '(label maplist (x f) (cond ((eq (car x) nil) nil) (_true (cons (f x) (maplist (cdr x) f))))) '())\n\n\n;;;; File: pat-match.lisp\n(in-package :lennma-math)\n\n;;;;\n;;\n;; Unification merges ... somwhat forgot it is somewhat-forgot.\n;; ... Um? what is somewhat-forgot? ...\n;; Oh.. I remember. somewhat-forgot is somewhat well known which is appered in another place,\n;; formed as the pair of temporary key and its content.\n;;\n;; (unification '(something) (Taro like the-Mu) (Taro like something) '())\n;; (unification '(x) '(a x 2) '(a 4 2) '()) ;; --> ((x . 4)) also (x)\n\n;;;; Utils\n\n(defun lwarn (control-string &rest format-arguments)\n  (let ((message (concatenate 'string \"!!Warn!!: \" control-string \"~%\")))\n    (apply #'format (append (list t message) format-arguments))))\n\n;;;; template unification engine\n\n(defconstant +fail-unify+ '+fail-unify+)\n;; todo: define incalculable. calculation divergence. by such as search_iter_limit.\n\n\n;;; basic function\n(defun unknownp (symbol unknowns)\n  \"is unknown symbol at pattern?\"\n  (find symbol unknowns :test 'equal))\n\n\n(defun recursive-bind-search (unknown binds &optional (path (list unknown)))\n  (let ((current-binded (cdr (car (remove-if-not #'(lambda (uu) (eql unknown (car uu))) binds)))))\n    ;; (format t \"~A, ~A~%\" current-binded path)\n    (cond ((null current-binded)\n           (reverse path))\n          ((member current-binded path :test #'equal)\n           (reverse (cons current-binded path)))\n          (t\n           (recursive-bind-search current-binded binds (cons current-binded path))))))\n\n\n;;;; binding functions\n\n\n(defun maybe-register-to-bind-ignore-name-confrict (name value unknowns binds)\n  (let* ((bind-same-name (find-if #'(lambda (nv) (equal (car nv) name)) binds)))\n    (cond ((not (unknownp name unknowns))\n           (lwarn \"~A is not unknown struct\" name)\n           +fail-unify+)\n          ((null  bind-same-name)\n           (cons (cons name value) binds))\n          ((equal (cdr bind-same-name) value)\n           binds)\n          (t +fail-unify+))))\n\n#|\n(defun maybe-register-to-bind (name value unknowns binds)\n  (let* ((LoL-s ;; it wil generate same struct of \"Let over Lambda\"-Series.\n           (recursive-bind-search name binds))\n         (car-bindto (cadr lol-s)))\n    ;; (format t \"R: ~A ~A~%\" name (unknownp name binds))\n    (cond ((not (unknownp name unknowns))\n           (lwarn \"~A is not unknown struct\" name)\n           +fail-unify+)\n          ((equal name value)\n           binds)\n          ((null car-bindto)\n           (cons (cons name value) binds))\n          ((equal value car-bindto)\n           binds)\n          ((and (not (equal value car-bindto))\n                (unknownp car-bindto unknowns))\n           (cons (cons name value) binds))\n          ((and (not (equal value car-bindto))\n                (not (unknownp car-bindto unknowns)))\n           ;; == t.\n           ;; decided struct appeared multiply different.\n           +fail-unify+))))\n|#\n\n;;;; matching\n\n\n(defun match-of-list^a1 (unknowns pattern-left pattern-right binds)\n  (cond ((unknownp pattern-left unknowns)\n         (maybe-register-to-bind-ignore-name-confrict pattern-left pattern-right unknowns binds))\n         ;;(maybe-register-to-bind pattern-left pattern-right unknowns binds))\n        ((eql pattern-left pattern-right)\n         binds)\n        (t +fail-unify+)))\n\n(defun match-of-list^an (unknowns pattern-left pattern-right binds)\n    (cond\n      ((unknownp (car pattern-left) unknowns)\n       (match-left unknowns (cdr pattern-left) (cdr pattern-right)\n                   (maybe-register-to-bind-ignore-name-confrict (car pattern-left) (car pattern-right) unknowns binds)))\n      (t\n       (match-left unknowns (cdr pattern-left) (cdr pattern-right)\n                   (match-left unknowns (car pattern-left) (car pattern-right) binds)))))\n\n(defun match-left (unknowns pattern-left pattern-right binds)\n  ;;(format t \"M: ~A, ~A~%\" pattern-left pattern-right)\n  (cond\n    ((eq binds +fail-unify+) +fail-unify+)\n    ((and (null pattern-left) (null pattern-right))\n     (values binds unknowns))\n    ;;\n    ((atom pattern-left)\n     (match-of-list^a1 unknowns pattern-left pattern-right binds))\n    ((and (listp pattern-left) (listp pattern-right))\n     (match-of-list^an unknowns pattern-left pattern-right binds))\n    (t +fail-unify+)))\n\n\n\n#|\n;;;; unification\n\n\n\n\n(defun unification-of-list^a1 (unknowns pattern1 pattern2 binds)\n  (let* ((binds-p1\n           (if (unknownp pattern1 unknowns)\n               (maybe-register-to-bind (car pattern1) (car pattern2) unknowns binds)\n               binds))\n         (binds-p2\n           (if (and (unknownp pattern2 unknowns) (not (eq binds-p1 +fail-unify+)))\n               (maybe-register-to-bind (car pattern2) (car pattern1) unknowns binds-p1)\n               binds-p1)))\n  (cond\n    ((or (unknownp pattern1 unknowns) (unknownp pattern2 unknowns))\n     binds-p2)\n    ((eql pattern1 pattern2) ;; pattern1 and pattern2 are decided\n     binds)\n    (t +fail-unify+))))\n\n(defun unification-of-list^an (unknowns pattern1 pattern2 binds)\n  (let* ((binds-p1\n           (if (unknownp (car pattern1) unknowns)\n               (maybe-register-to-bind (car pattern1) (car pattern2) unknowns binds)\n               binds))\n         (binds-p2\n           (if (unknownp (car pattern2) unknowns)\n               (maybe-register-to-bind (car pattern2) (car pattern1) unknowns binds-p1)\n               binds-p1)))\n    (cond\n      ((or (unknownp (car pattern1) unknowns) (unknownp (car pattern2) unknowns))\n       (unification unknowns (cdr pattern1) (cdr pattern2) binds-p2))\n      (t\n       (unification unknowns (cdr pattern1) (cdr pattern2)\n      (unification unknowns (car pattern1) (car pattern2) binds))))))\n\n\n  \n(defun unification (unknowns pattern1 pattern2 binds)\n  \"returns1: (unknown . its-value) list\nreturns2: unknowns\"\n  \n  ;; note: when both of pattern1 and pattern2 are unknown,\n  ;; this implement returns the set which has all binded unknowns at each pair's head of it's set.\n  ;; it means that returns are larger sized binds list than minimals.\n  \n  ;;(format t \"M: ~A . ~A . ~A~%\" pattern1 pattern2 binds)\n  (cond\n    ((eq binds +fail-unify+) +fail-unify+)\n    ((and (null pattern1) (null pattern2))\n     (values binds unknowns))\n    ;;\n    ((or (atom pattern1) (atom pattern2))\n     (unification-of-list^a1 unknowns pattern1 pattern2 binds))\n    ((and (listp pattern1) (listp pattern2))\n     (unification-of-list^an unknowns pattern1 pattern2 binds))\n    (t +fail-unify+)))\n|#\n\n\n;;;; matcing-utility\n\n(defmacro unpack-match (vars match-expr &body body)\n  \"Bind VARS from the alist result of MATCH-EXPR, only if match succeeds.\"\n  (let ((result-sym (gensym \"MATCH-RESULT-\"))\n        (v-sym (gensym \"MATCH-V-\")))\n    `(let ((,result-sym ,match-expr))\n       (when (not (eq ,result-sym +fail-unify+))\n         (destructuring-bind ,vars\n             (mapcar (lambda (,v-sym) (cdr (assoc ,v-sym ,result-sym))) ',vars)\n           ,@body)))))\n\n\n;;;; File: formal-system_1.lisp\n(in-package :lennma-math)\n\n\n;;;;\n\n(defparameter +fail+ '+fail+)\n\n;;;; utility\n\n(defun fail-message (message sym1 arguments)\n  (cond ((listp arguments)\n         (format t \"~&~A (~A ~{~A~^ ~})\" message sym1 arguments))\n        ((atom arguments)\n         (format t \"~&~A (~A ~A)\" message sym1 arguments)))\n  +fail+)\n\n\n(defun failp (x)\n  (eq x +fail+))\n\n(defmacro def-maybe-function (name lambda-list &body body)\n  (let ((g (gensym \"g-maybe-result-\")))\n    `(defun ,name ,lambda-list\n       ((lambda (,g)\n          (if (failp ,g)\n              (progn ;;(fail-message \"~&[FAIL] ~A (~{~A~^ ~})~%\" ',name (list ,@lambda-list))\n                (fail-message \"[FAIL]\" ',name (list ,@lambda-list))\n                ,g)\n              ,g))\n        (progn ,@body)))))\n\n;;;; .S defines\n\n(def-maybe-function ope-is-atom-p (object)\n  (atom object))\n\n(def-maybe-function ope-is-funct-p (object)\n  (listp object))\n\n(def-maybe-function ope-of (object)\n  (cond ((ope-is-funct-p object)\n         (car object))\n        (t ;; +fail+\n         +fail+)))\n\n(def-maybe-function arg-of (object)\n  (cond ((ope-is-funct-p object)\n         (cdr object))\n        (t ;; +fail+\n         +fail+)))\n\n(def-maybe-function arg-len (object)\n  (cond  ((ope-is-funct-p object)\n          (length (arg-of object)))\n         (t ;; +fail+\n          +fail+)))\n\n(def-maybe-function arg-nth-of (n object)\n  (cond ((not (ope-is-funct-p object))\n         +fail+)\n        ((< (arg-len object) n)\n         +fail+)\n        (t (nth (- n 1) (arg-of object)))))\n\n\n;;;; Type Utility\n\n\n;;;; Operators for Formalize\n\n;; 対象記号 - Object symbols\n;; 関数記号 - Function symbols\n;; 関係記号 - Relational symbols\n;; 論理記号 - Logical symbols\n;; 一階の変数 - First-order variables\n;; 二階の変数 - Second-order variables\n;; 対象式 - Object Formula\n;; 論理式 - Logical Formula\n\n\n;;;; Type 1 Term. (Natural Number)\n\n(defparameter .t1_.b_0 '.t1_.b_0 \".b_0 is Individual Constant\")\n(defparameter .set-of-type1-natural-number-symbols (list .t1_.b_0))\n\n(defmacro def-type1-symbols (&rest syms)\n  `(progn\n     ,@(mapcar (lambda (s) `(defparameter ,s (quote ,s))) syms)\n     (defparameter .set-of-type1-variable-symbols ',syms\n       \"the 2nd-order set of 1st-order variable names\")))\n\n(def-type1-symbols\n  .t1_.alpha .t1_.beta .t1_.gamma .t1_.delta .t1_.epsilon .t1_.zeta .t1_.eta .t1_.theta\n  .t1_.iota .t1_.kappa .t1_.lambda .t1_.mu .t1_.nu .t1_.xi .t1_.omicron .t1_.pi\n  .t1_.rho .t1_.sigma .t1_.tau .t1_.phi .t1_.chi .t1_.psi .t1_.omega)\n\n(defparameter .prime '.prime)\n\n(defun aa^type1-chain (object bottom-p)\n  (cond\n    ((and (ope-is-atom-p object)\n          (funcall bottom-p object))\n     t)\n    ((and (ope-is-funct-p object)\n          (= (arg-len object) 1)\n          (eq (ope-of object) .prime))\n     (aa^type1-chain (arg-nth-of 1 object) bottom-p))\n    (t nil)))\n\n(defun a^type1-closed-natural-number-symbol-p (x)\n  (member x .set-of-type1-natural-number-symbols :test #'eq))\n\n(defun a^type1-closed-natural-number-p (object)\n  \".t1_.b_0 | (.prime .t1_.b_0) | ... | (.prime (.prime...(.prime .t1_.b_0)...))\"\n  (aa^type1-chain object (lambda (x) (eq x .t1_.b_0))))\n\n(defun a^type1-variable-symbol-p (x)\n  (member x .set-of-type1-variable-symbols :test #'eq))\n\n(defun a^type1-opened-natural-variable-p (object)\n  \"because .t1_.xi is .type1 variable name, .t1_.xi | (.prime .t1_.xi) | ... | (.prime (.prime ...(.prime .t1_.xi)...))\nwhere .t1_.b_0 is not opened.\"\n  (aa^type1-chain object\n                  (lambda (x) (and (a^type1-variable-symbol-p x)))))\n\n(defun a^type1-term-p (object)\n  \"when .a is .t1_.b_0 or .type1-variable , .a | (.prime .a) | ... | (.prime (.prime...(.prime .a)...))\"\n  (cond ((or (a^type1-closed-natural-number-p   object)\n             (a^type1-opened-natural-variable-p object))\n         1)\n        (t nil)))\n\n\n\n;;;; Type n Term\n\n(defun a^type-n-symbol-p (sym)\n  \"If sym is a symbol named like .t<num>_.<name>,\nreturn two values: n as an integer, and the symbol whose string after '_.'.\nOtherwise return NIL.\"\n  (let* ((sym-name (if (symbolp sym) (symbol-name sym) nil)) ;; UPCASE (symbol-name x)\n         ;; must be form of \".t<num>_.<name>\"\n         (pos  (if (and sym-name\n                        (>= (length sym-name) 6)\n                        (string= \".T\" sym-name :end2 2))\n                   (search \"_.\" sym-name) nil)))\n    (when (and pos (> pos 2))\n      (let* ((num-pos-str (subseq sym-name 2 pos))\n             (maybe-num (if (every #'digit-char-p num-pos-str) (parse-integer num-pos-str) nil))\n             (maybe-namestr (intern (string-upcase (subseq sym-name (+ pos 2 -1))))))\n        (if (and (and (numberp maybe-num) (>= maybe-num 1)) (not (string= maybe-namestr \"\")) ;; .t<number>_.<string>\n                 (if (= 1 maybe-num)\n                     (a^type1-term-p sym) ;; if sym looks like .t1_.<name>, it is maybe type1 object.\n                     t))\n            (values maybe-num maybe-namestr)\n            nil)))))\n\n(defparameter +type_star+ '+type_star+)\n\n(defun a^type-star-symbol-p (sym)\n  \"If sym is a symbol named like .t*_.<name>,\nreturn two values: t/nil as symbol detection , and the symbol whose string after '.t*_.'.\nOtherwise return NIL.\"\n  (let* ((sym-name (if (symbolp sym) (symbol-name sym) nil)) ;; UPCASE (symbol-name x)\n         ;; must be form of \".t<num>_.<name>\"\n         (form-p  (and sym-name\n                       (>= (length sym-name) 6)\n                       (string= \".T*_.\" sym-name :end2 5)))\n         (maybe-namestr (if form-p (intern (string-upcase (subseq sym-name 4))) nil)))\n    (if (and form-p maybe-namestr)\n        (values +type_star+ maybe-namestr)\n        nil)))\n\n(defun a^type-n-term-p (n object)\n  (cond ((= n 1)\n         (let ()\n           (if (a^type1-term-p object) 1 nil)))\n        ((numberp n)\n         (let ((n_t2 (a^type-n-symbol-p object)))\n           (if (and (numberp n_t2) (= n n_t2))\n               n_t2 nil)))\n        ;;(t (error \"n in not number\") nil)))\n        ))\n\n(defun a^type-some_n-term-p (object)\n  \"returns ... 1 / some Type Number / +type_star+ / nil\"\n  ;; Future-Todo: detect variable type term such as .t{n}_.a , .t{x+1}_.b , t{u-1}_.c , .t{mod-eq u v 5}_.p ...\n  (or (a^type1-term-p object)\n      (a^type-n-symbol-p object)\n      (a^type-star-symbol-p object)))\n\n\n;;;; Relation (.in a b)\n\n(defparameter .in '.in)\n\n(defun a^.in_a_b-allowed-p (a b)\n  \"According to type levels, detect a .in b is allowed .\nwhere the a must be type-n object , n must be (<= n 1), the b must be type-{n+1} object.\"\n  ;; examples\n  ;; CL> (a^.in_a_b-allowed-p '.t1_.b_0 '.t2_.a) > 2\n  (multiple-value-bind (.n-of-a .name-of-a)\n      (a^type-n-symbol-p a)\n    (declare (ignore .name-of-a))\n    (if (and (integerp .n-of-a)\n             (>= .n-of-a 1))\n        (a^type-n-term-p (+ 1 .n-of-a) b)\n        nil)))\n\n(defun a^.atomic-formula-p (object)\n  \"s1 .in t1 , where s1 is n order-term, t1 is (n + 1) order-term\"\n  (unpack-match (?A ?B)\n    (match-left '(?A ?B) '(.in ?A ?B) object '())\n    (a^.in_a_b-allowed-p ?A ?B)))\n\n\n\n;;;; Logic Formula\n\n(defun a^.neg-formula-p (object)\n  \"(.neg A) , where A is logic-formula\"\n  (unpack-match (?A)\n      (match-left '(?A) '(.neg ?A) object '())\n    (a^logic-formula-p ?A)))\n\n(defun a^.to-formula-p (object)\n  \"(A .to B) , where A and B is logic-formula\"\n  (unpack-match (?A ?B)\n      (match-left '(?A ?B) '(.to ?A ?B) object '())\n    (and (a^logic-formula-p ?A)\n         (a^logic-formula-p ?B))))\n\n(defun a^.forall-formula-p (object)\n  \"(.forall x A) , where x is variable, A is logic-formula\"\n  (unpack-match (?x ?A)\n      (match-left '(?x ?A) '(.forall ?x ?A) object '())\n    (if (and (a^type-some_n-term-p ?x)\n             (a^logic-formula-p ?A))\n        (list ?x ?A) nil)))\n\n#|\n(defun a^logic-symbol-p (sym)\n  \"If sym is a symbol named like .l_.<name>,\nreturn two values: t/nil as symbol detection , and the symbol whose string after '.l_.'.\nOtherwise return NIL.\"\n  (let* ((sym-name (if (symbolp sym) (symbol-name sym) nil)) ;; UPCASE (symbol-name sym)\n         ;; must be form of \".l_.<name>\"\n         (form-p  (and sym-name\n                       (>= (length sym-name) 5)\n                       (string= \".L_.\" sym-name :end2 4)))\n         (maybe-namestr (if form-p (intern (string-upcase (subseq sym-name 3))) nil)))\n    (if (and form-p maybe-namestr)\n        (values t maybe-namestr)\nnil)))\n|#\n\n(defun a^logic-symbol-p (sym)\n  ;; todo Fix: optional parameter of binding-schemer-symbols : start from \"?\" such as ?A ?B ... ?HOGEHOGE\n  ;; such as (defun a^logic-symbol-p-added-binds (sym binds) ...)\n  ;; and also additional define to (defun a^logic-formula-p-added-binds (formula binds) ...)\n  \"If sym is a symbol named like .l_.<name>,\nreturn two values: t/nil as symbol detection , and the symbol whose string after '.l_.'.\nOtherwise return NIL.\"\n  (let* ((sym-name (if (symbolp sym) (symbol-name sym) nil)) ;; UPCASE (symbol-name sym)\n         ;; must be form of \".l_.<name>\"\n         (form-p  (and sym-name\n                       (>= (length sym-name) 5)\n                       (string= \".L_.\" sym-name :end2 4)))\n         (maybe-namestr (if form-p (intern (string-upcase (subseq sym-name 3))) nil))\n         ;; or be form of \"?\" ;; todo: fix\n         (form-p_?  (and sym-name\n                         (>= (length sym-name) 2)\n                         (string= \"?\" sym-name :end2 1)))\n         (maybe-namestr_? (if form-p_? (intern (string-upcase (subseq sym-name 1))) nil)))\n\n    (cond ((and form-p maybe-namestr)\n           (values t maybe-namestr))\n          ((and form-p_? maybe-namestr_?)\n           (values t maybe-namestr_?))\n          (t nil))))\n\n\n(defun a^logic-formula-p (object)\n  (cond\n    ((and (ope-is-funct-p object)\n          (or (a^.atomic-formula-p object) ;; 1. (.in s1 t1)\n              (a^.neg-formula-p object) ;; 2. (.neg A)\n              (a^.to-formula-p object) ;; 3. (.to A B)\n              (a^.forall-formula-p object))) ;; 4. (.forall x1 A)\n     t)\n    ;; ADD-1. in addition-1, if it is given logic-formula such as '.l_.BAZ , it is logic-formula.\n    ((and (ope-is-atom-p object)\n          (a^logic-symbol-p object)))\n    ;; Todo: Subst の再定義\n    ;; Todo: (mf^Subst A x) =:: F(x) の定義と、\n    ;; Todo: (me^Subst A x t1) =:: F(t1) の定義。\n    ;;\n    ;; この段落は比喩なので、そのまま実装してはならない。\n    ;; Lispパラダイムに例えると、論理変数のシンボルへと還元されていない評価可能なS式のAに対して\n    ;; (則ち、Aはletやlabelsの如き式から束縛をされていないこと。又は則ちenvによる置換を受けないpair又はsymbol)。\n    ;; x の特定の階数nが得られるとする。\n    ;; (me^Subst A x t1) は t1の階数がxの階数と等しい時にのみ定義され、Aの中の *自由変数として* 表れているxをt1で置換する。\n    ;;   このとき、Aの中で *束縛変数として表れているxをt1で置換してはならない*\n    ;; (mf^Subst A x) とは (lambda (t1) (me^Subst A x t1)) という\n    ;;   (me^Subst A x t1) を生成する為のlambda(超)関数のことを言う。\n    ;; ...\n    ;; (me^Subst A x t1) は展開後も論理式(又は論理式の要素)として論理式の内部へと表れるが ( F(t) なので ) 、\n    ;; (mf^Subst A x) も、その生成lambda(超)関数の式構造自体が、論理式として扱われる ( F(x) なので ) 。\n    ;;\n    ;; 実際には、lambda で包むことはせず、\n    ;; 1. (me^Subst A x t1) は推論装置の何れかの段階で、既に展開されている事を要求し、これは論理式ではない。\n    ;; 2. (mf^Subst A x) は F(x) の実際の表記なので、ライプニッツ表記風に、これのみを(超)論理関数F(x)の定義とし、論理式として認める\n    ;; 3. lambda項風の項については、common lisp風の式は、この体系では(形式的な)論理式とはしないことから、これもまた論理式としない。\n    ;;\n    ;;\n    ;; Old Memo:\n    ;; ADD-2. in addition-2, if it is legal (^Subst A x1 t1) form such as (^Subst .l_.A .t2_.u .t2_.x),\n    ;;     all of one Subst-formla and many expanded (Subst-)formlas are logic-formula.\n    ;;     (it is not expanded for detectinton now)\n\n    ((and (ope-is-funct-p object)\n          (m^^subst-formula-p object nil))\n     t)\n    (t nil)))\n\n\n;;;; Todo and Pass: Conversion of Infix and Prefix\n\n;;;; Todo: Substitute. Subst(F(x), x, t) means F(t)\n\n;;;; Todo: Conversion for Abbreviation such as .or .and .exists .iff .equal F(t)\n\n\n\n;;(defun assoc-all (item alist &test (test #'eql))\n;;  (remove-if-not (lambda (pair_i) (funcall test (car pair_i) item)) alist))\n\n(defun a^rebind-free-symbols (f1 x1 t1)\n  (let ((is-forall-formula? (a^.forall-formula-p f1)))\n    (cond ((and is-forall-formula? ;; form of (.forall x A)\n                (eql (car is-forall-formula?) x1))\n           f1)\n          ;; ToDo. if other operation is needed such as .exists, .iota-operation, .epsilon-operation, ... please add them to this handler\n          (nil t)\n          ;;\n          ((eql f1 x1)\n           t1)\n          ((consp f1) ;; ope-is-funct-p\n           (cons (a^rebind-free-symbols (car f1) x1 t1)\n                 (a^rebind-free-symbols (cdr f1) x1 t1)))\n          (t ;; ope-is-atom-p\n           f1))))\n\n\n(defun a^substitute (fx1 x1 t1)\n  (let (;; see also, m^^subst-formula-p\n        (logic-formula-p_fx1 (a^logic-formula-p fx1))\n        (var_type_x1 (a^type-some_n-term-p x1))\n        (var_type_t1 (a^type-some_n-term-p t1)))\n    (if (and logic-formula-p_fx1 var_type_x1 var_type_t1\n             (eql var_type_x1 var_type_t1))\n        (let ((new-formula-maybe (a^rebind-free-symbols fx1 x1 t1)))\n          (if (a^logic-formula-p new-formula-maybe)\n              new-formula-maybe\n              (fail-message \"Bad New-Substitute got\" 'a^substitute (list fx1 x1 t1))))\n        (fail-message \"Bad Argument for Substitute given\" 'a^substitute (list x1 t1)))))\n\n(defun m^^subst-formula-p (object &optional (expand? nil))\n  \"(^subst A x1 t1) , where A is logic-formula, both of x1 and t1 are type_n variable.\"\n  (unpack-match (?A ?x1 ?t1)\n      (match-left '(?A ?x1 ?t1) '(^subst ?A ?x1 ?t1) object '())\n    (if (and (a^logic-formula-p ?A) ;; A is logic formula\n             ((lambda (type_x1 type_t1) (and type_x1 type_t1\n                                             (eql type_x1 type_t1))) ;; n of type_n is must be same.\n              (a^type-some_n-term-p ?x1) ;; type_n variable\n              (a^type-some_n-term-p ?t1) ;; type_n variable\n              ))\n        (if expand?\n            (a^rebind-free-symbols ?A ?x1 ?t1)  ;; a^ evaluation type, returns.\n            object)                             ;; m^ evaluation type, returns.\n        nil)))\n\n\n;;;; Axioms\n\n;; defined at axioms_1.lisp\n\n\n\n;;;; Formal-Proof\n\n(defun immediate-consequence (provable-As)\n  provable-as nil\n  ;; 直接の結論\n  ;; 1. 論理式A と 論理式A.toB に対して、\n  ;;    A, A .to B .vdash B\n  ;;    の 論理式B を、論理式Aと論理式A->Bの直接の結論と言う。\n  ;; 2. 論理式A と 任意の階数の任意の変数.xiに対して、\n  ;;    A .vdash .forall.xiA\n  ;;    の論理式 .forall.xiA を、 Aの直接の結論と言う。\n\n  )\n\n\n(defun formal-proof (provable-As)\n  provable-as\n  ;; 8.2 証明の形式化\n  ;;\n  ;; 直接の結論\nnil\n  )\n\n\n\n\n\n\n\n\n;;;; Tests\n\n(defun test-of-a^type-some_n-term-p ()\n  (let* ((pairs-of-result-and-input\n           '((nil  . .t1_.fuga)                   ;; Nope. fuga is not allowed for type_1 name\n             (2    . .t2_.fuga)                   ;; OK. fuga is allowed more than type_2\n             (3    . .t3_.fuga)                   ;; OK.\n             (1    . .t1_.epsilon)                ;; OK. type1 .epsilon\n             (2    . .t2_.epsilon)                ;; OK. type2 .epsilon\n             (nil  . .epsilon)                    ;; Nope. .epsilon but type is not shown\n             (nil  . .hogehgoe)                   ;; Nope.\n             (nil  . .t0_.b_0)                    ;; Nope. type 0 is not allowd\n             (nil  . .t-2_.b_0)                   ;; Nope. type -2 is not allowd\n             (1    . .t1_.b_0)                    ;; Nope.\n             (2    . .t2_.b_0)                    ;; OK.\n             (3    . .t3_.b_0)                    ;; OK.\n             (1    . (.prime .t1_.b_0))           ;; OK. .prime of constant.b_0\n             (1    . (.prime (.prime .t1_.tau)))  ;; OK. .prime .prime of type1 variable\n             (+type_star+ . .t*_.tau)             ;; OK. type star symbol (1)\n             (+type_star+ . .t*_.fugafuga)        ;; OK. type star symbol (2)\n             (nil         . .t*_fugafuga)         ;; Nope. not type star symbol. it is dummy.\n             (137         . .t137_.tau)           ;; OK. type 137 .tau\n             (530000      . .t530000_.phi)        ;; OK. type 530000 .phi\n             )))\n    (format t \"test-of a^type-some_n-term-p~%\")\n    (format t \"[SUCC/FAIL]: ok : return <-- input.~%\")\n    (mapcar #'(lambda (pair)\n                (let* ((p_input (cdr pair))\n                       (p_ok (car pair))\n                       (p_return (a^type-some_n-term-p p_input)))\n                  (format t \"[~A]: ~A : ~A ~8T<-- ~A~%\"\n                          (if (equal p_ok p_return) \"SUCC\" \"FAIL\")\n                          p_ok p_return p_input )\n                  (if (equal p_ok p_return) nil pair)))\n            pairs-of-result-and-input)))\n\n\n(defun test-of-a^logic-formula-p ()\n  (let* ((pairs-of-result-and-input\n           '((t   . (.in .t1_.b_0 .t2_.X))                  ;; SUCC: 1st-order in 2nd-order\n             (nil . (.in .t1_.b_0 .t1_.alpha))              ;; FAIL: both 1st-order terms\n             (t   . (.in .t2_.X .t3_.Y))                    ;; SUCC: 2nd-order in 3rd-order\n             (t   . (.in (.prime .t1_.tau) .t2_.X))         ;; SUCC: (.prime applied number) .in type_2 something.\n             (t   . (.in (.prime .t1_.tau) .t3_.Y))         ;; FAIL: but, it is not .in type_3 something.\n             (nil . (.in .t2_.X .l_.B))                     ;; FAIL: 2nd-order in logic-formula\n             (nil . (.in .l_.B .t2_.x))                     ;; FAIL: logic-formula in 2nd-order\n             (t   . (.neg (.in .t1_.b_0 .t2_.X)))           ;; SUCC: negation of atomic formula\n             (nil . (.neg (.in .t1_.b_0 .t1_.alpha)))       ;; FAIL: negation of wrong atomic formula\n             (t   . (.to (.in .t1_.b_0 .t2_.X) (.in .t1_.alpha .t2_.Y)))        ;; SUCC: implication\n             (nil . (.to (.in .t1_.b_0 .t2_.X) (.in .t1_.b_0 .t1_.alpha)))      ;; FAIL: type mismatch in implication\n             (t   . (.forall .t1_.alpha (.in .t1_.alpha .t2_.X)))     ;; SUCC: universal quantifier 1st-order var\n             (t   . (.forall .t2_.X (.in .t1_.b_0 .t2_.X)))           ;; SUCC: universal quantifier 2nd-order var\n             (nil . (.forall .t2_.X (.in .t2_.b_0 .t2_.X)))           ;; FAIL: type mismatch at (.in s1 t1)\n             (t   . (.neg (.to (.in .t1_.b_0 .t2_.X) (.in .t1_.alpha .t2_.Y)))) ;; SUCC: negation of implication\n             (t   . (.forall .t1_.alpha\n                     (.neg (.in .t1_.alpha .t2_.X))))       ;; SUCC: universal + negation\n             (nil . ())                                     ;; FAIL: empty list is not a formula\n             (t   . .l_.A)                                  ;; SUCC: .l_<name> is given logic formula by assumption.\n             (nil . .hoge)                                  ;; FAIL: '.hoge' is not recognized term\n             (t   . (.to .l_.A .l_.B))                      ;; SUCC:\n             (nil . (.to .hoge .t2_.tau))                   ;; FAIL:\n             (t   . (.forall .t*_.x .l_.A))                 ;; SUCC: (additional conditions are not written in here (.l_.A)).\n             (t   . (^subst .l_.a .t2_.eta .t2_.tau))       ;; SUCC: OK . lazy ^subst form, where .l_.a is given logic-formula.\n             (nil . (^subst .hoge .t2_.eta .t2_.tau))       ;; FAIL: lazy ^subst form, but .hoge does not logical name.\n             (nil . (^subst .l_.a .t2_.eta .t3_.tau))       ;; FAIL: lazy ^subst form, but type_n does not equal.\n             )))\n    (format t \"test-of a^logic-formula-p~%\")\n    (format t \"[SUCC/FAIL]: ok : return <-- input.~%\")\n    (mapcar #'(lambda (pair)\n                (let* ((p_input (cdr pair))\n                       (p_ok (car pair))\n                       (p_return (a^logic-formula-p p_input)))\n                  (format t \"[~A]: ~A : ~A ~8T<-- ~A~%\"\n                          (if (equal p_ok p_return) \"SUCC\" \"FAIL\")\n                          p_ok p_return p_input )\n                  (if (equal p_ok p_return) nil pair)))\n            pairs-of-result-and-input)))\n\n\n(defun test-of-a^substitute ()\n  ;; test cases for (Subst A1 x1 t1).\n  (let* ((pairs-of-result-and-input\n           '(( (.in .t2_.a .t3_.y) .                         ;; simple subst\n              ((.in .t2_.x .t3_.y) .t2_.x .t2_.a))\n             ( (.forall .t1_.eta (.in .t1_.eta .t2_.omega)) .              ;; .t1_.eta is aleady binded.\n              ((.forall .t1_.eta (.in .t1_.eta .t2_.omega)) .t1_.eta .t1_.xi))\n             ( (.forall .t1_.beta (.in .t1_.xi .t2_.omega)) .              ;; .t1_.eta is not binded.\n              ((.forall .t1_.beta (.in .t1_.eta .t2_.omega)) .t1_.eta .t1_.xi))\n             ;; Todo: 1. and 2. : 1. ^subst formula it self and its expands / 2. NESTED ^subst :: (^subst in ^subst)\n             ;; 1. Both of one (^subst A1 x1 t1) formula itself and its expanded formulas are considered to be logic-formulas.\n             ;;    Timing manipulation is the first subject.\n             ;;    (manipulation such as Expand or Fold-in or other operations defined in Axiom or Inference-Rules or ......)\n             ;; 2. Manipulation of NESTED ^subst logic-formula.\n             ;;    How to handle (^subst A2 x1 t1) appearing inside (^subst A1 x1 t1).\n             ;;    especially, regarding name collision is the second subject.\n             (( (forall x (P x y z)) 'y 'b )      . (forall x (P x b z)))\n             (( (forall x (forall y (Q x y z))) 'z 'c ) . (forall x (forall y (Q x y c))))\n             ;; ToDo-Future: (P x y z) などを任意の述語とする。\n             ;; ( (forall .t2_.x (P x b z)) . <-- (substitute '(forall .t2_.x (P x y z)) 'y 'b ))\n             ;; の様に適用すると、 P に実際の実装と、真偽の定まる命題とが、異なる階層で存在する、という言語が定義される。\n             ;; P が実際の実装のみでなく、引数の属性が満たす定理に依っても動作する様になることから、ヤバそげな言語となる。\n             )))\n\n    (format t \"~&test-of a^substitute.~%\")\n    (format t \"~&[SUCC/FAIL]: ok : return <-- input ~%\")\n    (mapcar #'(lambda (pair)\n                (format t \"~%Hoge:~A~%\" (cdr pair))\n                (let* ((p_ok (car pair))\n                       (p_inputs (cdr pair))\n                       (p_return (apply #'a^substitute p_inputs)))\n                  (format t \"~&[~A]: OK_case : ~A~%  returned: ~A~%~%\"\n                          (if (equal p_ok p_return) \"SUCC\" \"FAIL\")\n                          p_ok p_return)\n                  (if (equal p_ok p_return) nil pair)))\n            pairs-of-result-and-input)))\n\n\n\n;;;; File: axioms_1.lisp\n(in-package :lennma-math)\n\n\n\n(defun make-axiom (&rest tmp-args)\n  (apply #'list tmp-args))\n\n\n;; テンプレートについてのメモ\n(defparameter *a^axiom_dummy*\n  (make-axiom\n   ;; ?A や ?B や ?x などは\n   ;; シンボル名チェックや、論理式として成立しているかのチェック(※推論後に論理式の解釈が破綻するかのチェックなどでは無い。)などは、\n   ;; (超操作の)代入後に行われるものとする。\n   :arguments '((?A . #'(lambda (v) v (a^generate-logic-formula-gen))) ;; ?A に指定が無き場合には、cdr部lambda項が生成とされる。\n                (?B . #'(lambda (v) v (a^generate-logic-formula-gen)))) ;; ?B に指定が無き場合には、<右記は上記と同様>...。\n   :before    `(and (a^logic-formula-p ?A) ;; expand by pair alists by (?A . 'symbol_of_A)\n                    (a^logic-formula-p ?B))\n   :formal    '(.to ?A (.to ?B ?A))\n   :after     #'identity\n   :amb-gen   #'identity))\n;;\n;; ?A や ?t2s や ?hogehoge などは実質的に、付帯条件検査が付属している、超操作 Subst(A, x, t) となる。\n;;\n;; 実際の実装の記述なしに、証明だけで定義されて記述される、オペレーションという、\n;; 証明付きのオペレーション生成パラダイムの土壌が、論理学には在ることになる。(自己言及の問題などなどは在るが。)\n;;\n;;\n\n\n;;;; A^axiom-I-* : Peano Axioms\n\n#|\n(defparameter *a^axiom-I-.t1_.b_0*\n  (make-axiom\n   :arguments '() ;; always '((?zero . #'(lambda (v) v (a^generate-type_1-closed-term .t1_.b_0))))\n   :before    t   ;; always `(a^logic-formula-p ?zero)\n   :formal    '(.in .t1_.b_0 .t2_.nat)  ;; .t2_.nat means set of natural-number :\n   ;;                               ;; in Imagine {0, 1, 2, 3,...}. but it is sets of nests of {.prime or final .t1_.b_0}.\n   :after     #'identity\n   :amb-gen   #'identity))\n\n(defparameter *a^axiom-I-.succ*\n  (make-axiom\n   :arguments `((?x . #'(lambda (v) v (a^generate-type_n-variable 1))))\n   :before    `(a^type1-term-p ?x)\n   :formal    `(.to (.in ?x .t2_.nat)\n                   (.in (.prime ?x) .t2_.nat))\n   :after     #'identity\n   :amb-gen   #'identity))\n|#\n\n(defparameter *a^axiom-I-1*\n  (make-axiom\n   :arguments `((?x . #'(lambda (v) v (a^generate-type_n-variable 1))))\n   :before    `(a^type1-term-p ?x)\n   :formal    `(.neg (.equal (.prime ?x) .t1_.b_0))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n(defparameter *a^axiom-I-2*\n  (make-axiom\n   :arguments `((?x . #'(lambda (v) v (a^generate-type_n-variable 1)))\n                (?y . #'(lambda (v) v (a^generate-type_n-variable 1))))\n   :before    `(and (a^type1-term-p ?x)\n                    (a^type1-term-p ?y))\n   :formal    `(.to (.equal (.succ ?x) (.succ ?y))\n                   (.equal ?x ?y))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n(defparameter *a^axiom-I-3*\n  (make-axiom\n   :arguments `((?t2s . #'(lambda (v) v (a^generate-type_n-variable 2))))\n   :before    `(a^type-n-term-p 2 ?t2s)\n   :formal    `(.to (.and (.in .t1_.b_0 ?t2s)\n                       (.forall .t1_.xi (.to (.in .t1_..xi ?t2s) (.in (.prime .t1_.xi) ?t2s))))\n                   (.forall .t1_.xi (.in .t1_.xi ?t2s)))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n\n\n;;;; A^axiom-II-* : Propositional Logic's Axioms\n\n(defparameter *a^axiom-II-1*\n  (make-axiom\n   :arguments `((?A . #'(lambda (v) v (a^generate-logic-formula-gen)))\n                (?B . #'(lambda (v) v (a^generate-logic-formula-gen))))\n   :before    `(and (a^logic-formula-p ?A)\n                    (a^logic-formula-p ?B))\n   :formal    '(.to ?A (.to ?B ?A))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n(defparameter *a^axiom-II-2*\n  (make-axiom\n   :arguments `((?A . #'(lambda (v) v (a^generate-logic-formula-gen)))\n                (?B . #'(lambda (v) v (a^generate-logic-formula-gen)))\n                (?C . #'(lambda (v) v (a^generate-logic-formula-gen))))\n   :before    `(and (a^logic-formula-p ?A)\n                    (a^logic-formula-p ?B)\n                    (a^logic-formula-p ?C))\n   :formal    `(.to (.to ?A (.to ?B ?C))\n                   (.to (.to ?A ?B) (.to ?A ?C)))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n(defparameter *a^axiom-II-3*\n  (make-axiom\n   :arguments `((?A . #'(lambda (v) v (a^generate-logic-formula-gen)))\n                (?B . #'(lambda (v) v (a^generate-logic-formula-gen))))\n   :before    `(and (a^logic-formula-p ?A)\n                    (a^logic-formula-p ?B)\n                    ;; Todo: A is proposition, B is proposition : detected into True/False.\n                    t ;; detection ?A\n                    t ;; detection ?B\n                    )\n   :formal    `(.to (.to (.neg ?B) (.neg ?A))\n                   (.to ?A ?B))\n   :after     #'identity\n   :amb-gen   #'identity))\n\n\n;;;; A^axiom-III-* : Predicate Logic's Axioms\n\n;; memo\n;; (unpack-match (?A ?B)\n;;     (match-left '(?A ?B) '(.in ?A ?B) object '())\n;;   (a^.in_a_b-allowed-p ?A ?B))\n\n(defparameter *a^axiom-III-1*\n  (make-axiom\n   :match-vars `(?F ?x ?t)\n   :match-expr `(.to (.forall ?x (?F ?x))\n                    (^subst ?F1 ?x ?t))\n   :let*_1      `((al^type_n (a^type-some_n-term-p ?x)))\n   :type-detect `((?F . #'(lambda (x) x (m^^subst-formula-p ?F ?x ?t)))\n                  (?x . #'(lambda (x) x al^type_n)) ;; == (a^type-some_n-term-p ?x)\n                  (?t . #'(lambda (x) x (a^type-n-term-p))))\n   :generator   `((?F . #'(lambda (x) x (m^generate-subst-formula-gen)))\n                  (?x . #'(lambda (x) x (if type_n (a^generate-type_n-variable al^type_n) +fail+)))\n                  (?t . #'(lambda (x) x (if type_n (a^generate-type_n-variable al^type_n) +fail+))))\n   :let*_2      `((al^after-subst (a^substitute ?x ?x ?t)))\n   ;; al^__type-detect__\n   ;; al^__proven-logic-formulas--\n   ;; ;; don't use common lisp variable named al^__<foobar>__ such as al^__type-detect__\n   ;; ;; they are used in expanded S-formula. (todo: make warning by intern-ed name at :let*_1 / :let*_2)\n   :before      `(and al^__type-detect__ ;; todo: (forall (td as :type-detect) (td is not (+fail+ or nil)))\n                      t ;; (a^logic-formula-p al^after-subst) is detected inner (a^substitute A1 x1 t1)\n                      )\n   :formal      '`(.to (.forall ,?x (,?F ,?x))\n                      ,after-subst)\n   :after       #'identity))\n\n#|\n;; memo\n;;   :match-expr `(.to (.forall ?x (?F ?x)) (^subst ?F1 ?x ?t))\n;; (^subst の展開後の形が全てmatchするハズという仕様だが、未来を先取りして未知数を落とすか、全部の組合せを列挙するか、\n;;    (例:女神に解の公式を教えて貰う、白色い直線光に対する虹色の分光(但しコンパクトコンパクトな光だったり、テンソル的な光、など)、など)\n;; などの機能の実装が必要で、そんな機能の実装は超自然的か無限計算機の産物なので)\n;; 一見して 上記の式の ^Substitute 展開後の式などほぼ全てが match しないのだが (展開前の^subst式ならまぁまぁmatchしそうだが)、\n;; もしも推論演算子 .vdash (implict) の実装が正しくできているのなら、 後段の推論の推論結果の超操作から、\n;;    :match-expr `(.to (.forall ?x (?F ?x)))\n;; の様な部分だけでmatchさせられる推論法則を、(超)定理として取り出せる様になる。\n;;\n;; ここで言う(超)定理とは、(多分おおよそ)一般的な数学で言う定理のことであり、\n;; 任意の定義域内の変数群に対して、或る性質が必ず成立するという主張が、証明付きで付与されているものであり、\n;; 実際に定義域内の変数群をその証明の証明図(証明グラフ)に当て填めてみると(経験的には)必ず、\n;; その構造の通りに証明付きでその主張が得られるものであり、\n;; 度々、人の手にかかると、その信頼感覚から証明図の追跡が省略されて、\n;; その定義域内の変数に対して、その主張へと段飛ばしに跳躍させられるものだが、\n;; 実際には推論規則そのものやルール抽出そのものの妥当性が議論の範囲外(体系内の言語から表現する能力の不在、などなど)となることから、\n;; 実際には経験則となるもののことである。\n;;\n;; まぁ例えば、或る数学体系Iの中での数学体系Iそのものの定義に於いて、未来に演繹されるハズの数学体系Iの命題を利用しても問題無い様な、\n;; 現在に未来を落とせる数学体系が、十分に動作するなら、(十分に動作する、とは、どういう事なのかについては、ここでは議論しないが。)\n;; 言い換えると、自己言及的な数学体系であったり、或いは、数学が陰陽図となっている場合(?)などには、\n;; (或る数学体系Iが、完全で自己完結的な場合、自己言及的で十分に動作する場合、陰陽図となっている場合、などには)\n;; 話は大分変わってくるであろうが、、、。\n;;\n|#\n\n\n(defparameter *a^axiom-III-2*\n  (make-axiom ;; forall x (A -> F (x)) -> (A -> forall x F(x))\n   :match-vars `(?x ?A ?F)\n   \n   #|\n   ;; todo\n   :match-expr `(.to (.forall ?x (?F ?x))\n                    (^subst ?F1 ?x ?t))\n   :let*_1      `((al^type_n (a^type-some_n-term-p ?x)))\n   :type-detect `((?F . #'(lambda (x) x (m^^subst-formula-p ?F ?x ?t)))\n                  (?x . #'(lambda (x) x al^type_n)) ;; == (a^type-some_n-term-p ?x)\n                  (?t . #'(lambda (x) x (a^type-n-term-p))))\n   :generator   `((?F . #'(lambda (x) x (m^generate-subst-formula-gen)))\n                  (?x . #'(lambda (x) x (if type_n (a^generate-type_n-variable al^type_n) +fail+)))\n                  (?t . #'(lambda (x) x (if type_n (a^generate-type_n-variable al^type_n) +fail+))))\n   :let*_2      `((al^after-subst (a^substitute ?x ?x ?t)))\n   ;; al^__type-detect__\n   ;; al^__proven-logic-formulas--\n   ;; ;; don't use common lisp variable named al^__<foobar>__ such as al^__type-detect__\n   ;; ;; they are used in expanded S-formula. (todo: make warning by intern-ed name at :let*_1 / :let*_2)\n   :before      `(and al^__type-detect__ ;; todo: (forall (td as :type-detect) (td is not (+fail+ or nil)))\n                      t ;; (a^logic-formula-p al^after-subst) is detected inner (a^substitute A1 x1 t1)\n                      )\n   :formal      '`(.to (.forall ,?x (,?F ,?x))\n                      ,after-subst)\n   :after       #'identity))\n|#\n))\n\n\n\n\n;; Todo: バツ\n(defparameter *a^axiom-III-1*\n  (make-axiom\n   :arguments `((?F1 . #'(lambda (v) v (m^generate-subst-formula-gen)))\n                ;; todo +type_star+ into +type_n+ , where n is n of some number.\n                ;; (Subst A1 x1 t1) における x1 の扱いは?\n                (?t*t1 . #'(lambda (v formal) v ;; formal defined at :formal below.\n                             (unpack-match (??t ??t*x1)\n                                 (match-left '(??t ??t*x1)\n                                             `(.to (.forall ?t*x1 (?F1 ??t*x1))\n                                                  (^subst ?F1 ??t*x1 ??t)) ;; todo: temporary .\n                                             formal '()\n                                             ;; todo: or\n                                             ;; global variable !LOL--formal-here--! .\n                                             ;; or alist '() may be helpfull.\n                                             ;; or lambda list may be helpfull.\n                                             ;; or :pat-binds\n                                             )\n                               ??t ;; ??t*x1\n                               (let ((maybe-level (a^type-some_n-term-p ??t*x1)))\n                                 (if maybe-level (a^generate-type_n-variable maybe-level) +fail+))))))\n   :before `(and (m^^subst-formula-p ?F)\n                 ;; (a^logic-formula-p (a^substitute ?F ?x1 ?t1))\n                 ;; and addition\n                 \n                 \n                 \n                 )\n   :formal    `(.to (.forall .t*_.x (?F1 .t*_.x)) ;; todo .t*_. of .t*_. :: 特定の自然数 n と 任意の自然数 n とは異なるものである。\n                   ;; ,(a^substitute ?F1 .t*_.x ?t)) ;; todo: correct.\n                   (^subst ?F1 .t*_.x ?t*t1)) ;; todo: temporary .\n   :after     #'identity\n   :amb-gen   #'identity))\n\n\n;; Todo: バツ\n(defparameter *a^axiom-III-2*\n  (make-axiom\n   :pat-binds `((?x) (todo)) ;; todo\n   :arguments `((?A . #'(lambda (v) v (a^generate-logic-formula-gen)))\n                (?F . #'(lambda (v) v (m^generate-subst-formula-gen))))\n   :before `(and (a^logic-formula-p ?A)\n                 (m^^subst-formula-p ?F)\n                 (a^type-some_n-term-p x)\n                 t nil ;; Todo : 付帯条件 として A は 変数 x を自由変数としては含まない。\n                 )\n   :formal `(.to (.forall ?x (.to ?A (^subst ?F ?x)))\n                (.to ?A (.forall ?x (^subst ?F ?x))))\n   :after #'identity\n   :amb-gen #'identity ;; todo. AMB timing\n   ))\n\n\n\n\n;;;; A^axiom-IV-1 : Axiom of Comprehension\n\n;; ToDo : implement\n(defparameter *a^axiom-iv-1*\n  (make-axiom\n\n   :before `(and nil ) ; type_x type_y (= (type x) (1+ (type y)))\n   :formal `(.exists x (.forall y (.iff (.in x y)\n                            (^subst x t1))))\n   :after 't1 ;\n   :amb-gen #'identity\n   ))\n\n\n\n;;;; A^axiom-V-1 : Axiom of Extensionality\n\n;; ToDo : implement\n(defparameter *a^axiom-v*\n  (make-axiom\n\n   :before `(and nil )\n   :formal `(.to (.forall .t*_.xi ;; todo: * is not n\n                    (.iff (.in .t*_.xi ?t{n+1}_.xi)\n                        (.in .t*_.xi ?t{n+1}_.eta)))\n                (.equal ?t{n+1}_.xi ?t{n+1}_.eta))\n\n   :after #'identity ;todo\n   :amb-gen #'identity\n   ))\n\n\n;;;; Inference Rules :\n\n(defun make-inference-rule (&rest tmp-args)\n  ;; froms_1 froms_2 ... froms_n ;; as list\n  ;; --------------------------- ;; .vdash\n  ;; intos_1 intos_2 ... intos_m ;; as list\n  (apply #'list tmp-args))\n\n;; todo\n(defparameter *a^inference-1*\n  (make-inference-rule\n   :before `(and t t)\n   :froms `((?A) (.to ?A ?B))\n   :intos `(?B)))\n\n;; todo\n(defparameter *a^inference-2*\n  (make-inference-rule\n   :before `(and t t)\n   :froms `((?A))\n   :intos `((.forall .t*_.x ?A))))\n\n\n\n\n\n;;;; File: gen-proof-net.lisp\n(in-package :lennma-math)\n\n(defun report-step (k-num queue-len current-form-str)\n  (let ((js-fn (jscl::oget jscl::*root* \"onLennmaSearchStep\")))\n    (unless (or (jscl::js-undefined-p js-fn)\n                (jscl::js-null-p js-fn))\n      (funcall js-fn k-num queue-len current-form-str))))\n\n\n\n;;;; References\n;; [kiso_1] 前原昭二: 『数学基礎論入門』朝倉書店 (初版 1977; 復刻第10刷 2017)\n;; -  (Shoji Maehara: \"The Foundations of Mathematics\" . Asakura (1977))\n;;\n;; In this file, especially rfer to:\n;;   Sections 1.3 and 8.2.\n\n\n;;;; Words\n;; 前提 - premise\n;; 結論 - conculsion\n;; 推論 - inference\n\n\n;;;; Structure of Node\n\n(deftype logic-type ()\n  '(member\n    l^axiom        ;; 公理 (Axiom).\n    l^hypoaxioma   ;; 「公理Kからの証明」 で言う前提 (Assumption). 仮の(Hypo-) 公理(Axioma).\n    l^lemma        ;; 小定理 (Lemma) および 定理 (Theorem).\n    l^hypothesis   ;; 「結論から前提への推論」で言う仮説 (Hypothesis / Subgoal).\n    l^temporary    ;; other\n    ))\n\n(defstruct formal-node\n  (logic-type 'l^hypothesis :type logic-type)\n  (k-number   -1            :type integer)\n  (vars       '())\n  (formal     '()))\n\n\n(defun make-axiom_1 (&key vars formal)\n  (make-formal-node\n   :logic-type 'l^axiom\n   :vars        vars\n   :formal      formal))\n\n\n;;;; Numbering\n\n#|\n(defparameter *k-number* 0)\n\n(defun reset-k-numer ()\n  (setq *k-number* 0))\n\n(defun formal-node-give-k-number-unsafe! (formal-node)\n  (progn\n    (setf (formal-node-k-number formal-node) *k-number*)\n    (setq *k-number* (+ 1 *k-number*))\n    *k-number*))\n|#\n(defun formal-node-give-k-number (formal-node k-number_to-node)\n  (let ((cp_1 (copy-formal-node formal-node)))\n    (progn (setf (formal-node-k-number cp_1) k-number_to-node)\n           (values cp_1 (+ 1 k-number_to-node)))))\n\n;;;; Axioms\n\n(defparameter *a-II-1*\n  (make-axiom_1\n   :vars `(?A ?B)\n   :formal `(.to ?A (.to ?B ?A))))\n\n(defparameter *a-II-2*\n  (make-axiom_1\n   :vars `(?A ?B ?C)\n   :formal `(.to (.to ?A (.to ?B ?C))\n                (.to (.to ?A ?B) (.to ?A ?C)))))\n\n(defparameter *a-II-3*\n  (make-axiom_1\n   :vars `(?A ?B)\n   :formal `(.to (.to (.neg ?B) (.neg ?A)) (.to ?A ?B))))\n\n(defparameter *axiom-list-II*\n  (list *a-ii-1* *a-ii-2* *a-ii-3*))\n\n\n;;;;\n\n(defun kp^is-axiom? (|A_k| |K-axioms|)\n  \"暗黙の前提: :logic-type が 'l^axiomであること。\"\n  (remove\n   nil\n   ;; |A_k| が(命題論理の範囲で)公理であるとは、\n   (append\n    ;; 1. シューマーそのものと一致するか?\n    (mapcar #'(lambda (a_n) ;; todo-future - list all of it.\n                (if (not (eql (formal-node-logic-type a_n) 'l^axiom))\n                    (progn\n                      #| (warn \"it's not l^axiom\") |#\n                      nil)\n                    (let ((match-p (match-left (formal-node-vars a_n)\n                                               (formal-node-formal a_n)\n                                               (formal-node-formal |A_k|)\n                                               '())))\n                      (if (and (not (eql match-p +fail-unify+))\n                               (every #'a^logic-formula-p (mapcar #'cdr match-p))) ;; ((alias . value) ... )\n                          (list :formal a_n :binds match-p) nil))))\n            |K-axioms|)\n    #|\n    ;; 2. 一致するシューマー内の全ての変数が、公理であるか?、である。\n    nil ;; ==> 2. は不要: 1. であるなら 2. は必ず満たされる為。\n    ;; ※. 逆に、生成時の .l_.foobar は任意の論理式であること。\n    |#\n    )))\n\n(defun kp^system-k-member-p (node)\n  (member (formal-node-logic-type node)\n          '(l^axiom l^hypoaxioma)))\n\n(defun kp^formula-equal-p (node-pattern node-into)\n  (not (eql (match-left '()\n                        (formal-node-formal node-pattern)\n                        (formal-node-formal node-into)\n                        '())\n            +fail-unify+)))\n\n(defun set-equal-p (xs ys &key (test #'equal))\n  (and\n   (every #'(lambda (x) (member x ys :test test)) xs)\n   (every #'(lambda (y) (member y xs :test test)) ys)))\n\n(defun kp^equals? (formal-node_1 formal-node_2)\n  ;; todo: in old implement this use |A_k| and K-assum_i\n  (and (formal-node-p formal-node_1)\n       (formal-node-p formal-node_2)\n       (a^logic-formula-p (formal-node-formal formal-node_1))\n       ;;\n       ;;(a^logic-formula-p (formal-node-formal formal-node_2)) ;;auto detect\n       (a^logic-formula-p (formal-node-formal formal-node_1))\n       (kp^formula-equal-p formal-node_1 formal-node_2)\n       t ;; ok. (a^logic-formula-p |A_k|)\n       (set-equal-p (formal-node-vars formal-node_1) (formal-node-vars formal-node_2))\n       ;; (binds-equal-p (formal-node-vars |A_k|) (formal-node-vars K-assum_i)) ;; todo\n       ))\n\n(defun kp^some-equals? (|A_k| |K-assumptions|)\n  (if (and (formal-node-p |A_k|)\n           (a^logic-formula-p (formal-node-formal |A_k|)))\n      (mapcar-remove-nil #'(lambda (K-assum_i)\n                             ;; todo: 「A_k は K の元であるか」 についての再解釈。\n                             ;; equalp と等価な match-left だけではなく、\n                             ;; (命題論理の範囲だけでも)シューマでもありえるとは考えられないか。\n                             (if (and t ;; (kp^system-k-member-p K-assum_i) ; todo: fix l^* type at searth \n                                      (kp^formula-equal-p |A_k| K-assum_i)\n                                      t ;; ok. (a^logic-formula-p |A_k|)\n                                      (set-equal-p (formal-node-vars |A_k|) (formal-node-vars K-assum_i))\n                                      ;; (binds-equal-p (formal-node-vars |A_k|) (formal-node-vars K-assum_i)) ;; todo\n                                      )\n                                 (list :formal K-assum_i :binds k-assum_i)\n                                 nil))\n                         |K-assumptions|)\n      nil))\n\n\n\n\n(defun kp^is-IC?_inference-rule1 (|A_k| |A-before-k|)\n  ;; {A, (.to A B)} .vdash B について。 命題論理での範囲にある 推論法則I のこと。\n  ;; .vdash の左から右の方向に向かって cartesian積 をつくって マッチングを行うと、計算爆発を起こすので (O ~= O(k^2))、\n  ;; .vdash の右から左の方向へとマッチングを行う (O ~= O(2*k))。\n  (mapcar-remove-nil-flatten\n   ;; A_i では V を A_k で束縛して、(.to U V) で A_k-befores をマッチング そして フィルターし、\n   ;; そのフィルターされた U のそれぞれが A_j とマッチするか調べる\n   #'(lambda (|A_i-may-.toUV|)\n       (unpack-match (?U)\n           (match-left '(?U) `(.to ?U ,(formal-node-formal |A_k|)) (formal-node-formal |A_i-may-.toUV|) '())\n                (mapcar-remove-nil\n                 #'(lambda (|A_j-may-U|)\n                     (unpack-match ()\n                         (match-left '() ?U (formal-node-formal |A_j-may-U|) '())\n                       ;; note: when A_k is a, A_i is (a -> a) -> a. self matching is not appeared.\n                       ;;`(h^ands ,|A_j-may-U| ,|A_i-may-.toUV|)))\n                       (list :formal `(h^ands ,|A_j-may-U| ,|A_i-may-.toUV|)\n                             :binds nil)))\n                 |A-before-k|)))\n   |A-before-k|))\n\n\n(defun kp^is-IC? (|A_k| IC-detectors |A-before-k|)\n  #| ;; todo: parse additional IC-detectors\n  (mapcar-remove-nil-flatten\n   #'(lambda (detect-function) (funcall detect-function |A_k| |A-before-k|))\n  IC-detectors\n  |#\n  (declare (ignore IC-detectors))\n  (identity (kp^is-ic?_inference-rule1 |A_k| |A-before-k|)))\n\n\n#|\n(defun kp^is-IC? (|A_k| IC-detectors A_k-befores)\n  ;; 3. Immediate Consequence\n  ;; 3. (i < k, j < k) として、A_k は A_k より以前に表れる\n  ;; 3_a. A_i による直接の結論であるか、\n  ;; 3_b. A_i と A_j による直接の結論であるか。\n  ;; 3_*. todo-future: その他の推論規則による直接の結論であるか。\n  ;;\n  ;;\n  ;; 例えば IC-i が (?A ?B) に対し {?A, (.to ?A ?B)} .vdash ?B のとき、\n  ;; まず |A_k| を用いて |A|_before に対して、 ?A と (.to ?A |A_k|) でパターンマッチングを行う。 :: (ex_1)\n  ;; (ex_1) の 左側の ?A と 右側の ?A が同じ論理式である組み合わせのとき、\n  ;; その ?A と\n  ;;\n  ;; 例えば IC-i が (?A) に対し ?A .vdash {(.forall .xi ?A)} のとき、\n  ;; まず、|A_k| に対して\n  nil\n  )\n|#\n\n\n(defun kp^is-proof? (|A_k| |K-axioms| |K-assumptions| |A-proven| |K-immediate-consequences|)\n  ;; K-証明 であるかを以下の仮定のもと確認する。\n  ;; |A|_before が全て証明であると仮定したとき、\n  ;; A_k (証明の列の末尾に表れた論理式) がKから証明できるとは、\n  (let*\n      ((|A|_before (safe-append |K-axioms| |K-assumptions| |A-proven|))\n       ;; 1. A_k は 公理であるか。\n       (is-axiom? (kp^is-axiom? |A_k| |K-axioms|))\n       ;; 2. A_k は K の元であるか。\n       (is-K? (kp^some-equals? |A_k| (safe-append |K-axioms| |K-assumptions|)))\n       ;; 3. A_k は直接の結論(Immediate Consequence)である。\n       (is-IC? (kp^is-IC? |A_k| |K-immediate-consequences| |A|_before)))\n    ;; の 1. 2. 3. のいづれかが成立することを言う。\n    (remove nil (safe-append is-axiom? is-K? is-IC?))\n    ))\n\n\n\n;;;;;;\n\n(defun syntheisis-to-axiom  (axiom node-j sub-nodes)\n  (let* ((axiom-formal  (formal-node-formal axiom))\n         (axiom-vars    (formal-node-vars   axiom))\n         (node-j-formal (formal-node-formal node-j))\n         (patterns-1dim (cons node-j sub-nodes))\n         ;; memo. 型的に危いので、:formalだけをとりだして:formalの組のlistだけで出力させることは、\n         ;; しないこと。 formal-node の 組 の list であること。\n         (cart-all-patt (cartesian (make-list (length axiom-vars) :initial-element patterns-1dim))))\n    (mapcar-remove-nil\n     #'(lambda (cart_i-nodes)\n                           (let* ((cart_i-formals (mapcar #'formal-node-formal cart_i-nodes))\n                                  (maybe-one-axiom ;; applied schema. :: スキーマの固定\n                                    ;; (sublis alist tree &key key test test-not)\n                                    ;; ((f^-1 match-left) vars .......)\n                                    (sublis (pairlis axiom-vars cart_i-formals)\n                                            axiom-formal))\n                                  (node_i (make-formal-node :logic-type 'l^hypothesis :vars '() :formal maybe-one-axiom)))\n                             (cond ((not (member node-j-formal cart_i-formals :test #'equalp))\n                                    ;; ;; todo: 1回以上 node-j の形式が出現すること。 への事前filter。\n                                    nil)\n                                   ((kp^is-axiom? node_i (list axiom)) node_i)\n                                   (t nil))))\n            cart-all-patt)))\n\n\n;; > (syntheisis-to-axiom *a-ii-1* ;; vars are (?A ?B) 's 2 parameters.\n;;                     (make-formal-node :formal '.l_.y)\n;;                     (absorb-sub-logic-node-list (list\n;;                                                  (make-formal-node :formal '(.to (.to .l_.c .l_.x) (.neg .l_.b)))\n;;                                                  (make-formal-node :formal '(.to .l_.a (.neg .l_.b))))))\n;; < (list of #S(FORMAL-NODE ......))\n;; ;; 仮にabsorb-...の出力の要素数が8だったとする。\n;; < (length of-it) may 17 (= 1 + 8*2 + 0 + ... + 0)\n;; ;; where (?A ?B ?C) => 217 ;; (= 1 + 8*3 + 8*8*3 + 0 + ... + 0)\n;; ;; todo: そう言えば、2025 を 5 で 3回以上割る問題が在ったような、、、。\n\n(defun syntheisis-to-axioms (|K-axioms| |A_k| sub-formal-nodes)\n  ;; s_axiom-generates (syntheisis-to-axioms |K-axioms| |A_k| sub-formal-nodes))\n  (reduce #'safe-append (mapcar\n                   #'(lambda (axiom_i) (syntheisis-to-axiom axiom_i |A_k| sub-formal-nodes))\n                   |K-axioms|)\n          :initial-value nil))\n\n;; >(length (syntheisis-to-axioms *axiom-list-ii*\n;;   (make-formal-node :formal '.l_.y)\n;;                     (absorb-sub-logic-node-list (list\n;;                                                  (make-formal-node :formal '(.to (.to .l_.c .l_.x) (.neg .l_.b)))\n;;                                                  (make-formal-node :formal '(.to .l_.a (.neg .l_.b)))))))\n;; < 251 ;; 217 + 17 + 17 for an example.\n\n(defun syntheisis-add-immediate-consequence (rules |A_k| |A|)\n  ;; A_k ++ |A| ** |A| にて\n  (declare (ignore rules))\n  ;; todo: parse rule, parse rules\n  (let* ((synth-by-rule-I\n           (mapcar-remove-nil #'(lambda (pair)\n                                  ;;(format t \"~%P:~A~%\" (print pair))\n                                  (inference-top_to_down-rule-I (car pair) (cdr pair))\n                                  )\n                              ;;(cartesian-2 `(,|A_k|) |A|)))\n                              (cartesian-2 `(,|A_k|) (cons |A_k| |A|))))\n\n         ;; todo\n         (synth-by-rule-II\n           nil)\n         ;;\n         (syntheisis-list (remove nil (safe-append synth-by-rule-I synth-by-rule-II))))\n    (reduce #'safe-append syntheisis-list :initial-value nil)\n    ))\n\n\n(defun inference-top_to_down-rule-I (formal-node_1 formal-node_2)\n  ;; h{ A h, (.to A B) h} h.vdash B\n  ;; symbols beginning with h means symbols at hyper layer (when look from formal world).\n  (let* (;; formal-formula.\n         (ff_1 (formal-node-formal formal-node_1)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         (ff_2 (formal-node-formal formal-node_2)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         ;; super-layer formulas: outer formal-formula.\n         ;;   (sl_1 (......)) ;; todo\n         ;;   (sl_2 (......)) ;; todo\n         ;; forms\n         (ff_ands `(h^ands ,ff_1 ,ff_2))\n         (sl_addition '()) ;; todo\n         ;; templates\n         )\n    (declare (ignore sl_addition)) ;; todo: detection with additional conditions\n    #|\n    (print\n     (match-left '(?A ?B)\n                 '(h^ands ?A (.to ?A ?B))\n                 ff_ands '()))\n    |#\n    ((lambda (_x) (if _x (list _x) ())) ;; [_x] or [] ;; one-formulas or none-formulas\n     (unpack-match (?A ?B)\n         (match-left '(?A ?B) '(h^ands ?A (.to ?A ?B)) ff_ands '()) ;; template for formal-formula also\n       (declare (ignore ?A))\n       ;; todo: this is only formal world. implement additional conditions in hyper layer\n       (make-formal-node :logic-type 'l^hypothesis :vars '()\n                         :formal ?B))\n     )))\n\n#|\n(match-left '(?A ?B)\n            '(h^ands ?A (.to ?A ?B))\n            '(h^ands (.to .L_.A (.to .L_.B .L_.C))\n              (.to (.to ?A (.to ?B ?C)) (.to (.to ?A ?B) (.to ?A ?C)))))\n\n\n;;            '(h^ands u (.to u v))\n;;            '())\n|#\n\n\n\n(defun absorb-sub-logic-node (formal-node_x)\n  ;; todo: formal-language safe parse.\n  (labels ((aux (cxxxr)\n             (cond ((null cxxxr)         nil)\n                   ((not (listp cxxxr))  (list cxxxr))\n                   (t                    (cons cxxxr (apply #'append (mapcar #'aux cxxxr)))))))\n    ;; todo: シューマーである場合についての考慮。現在はシューマー(のシューマー)は公理でないとしている。\n    (if (not (null (formal-node-vars formal-node_x)))\n        nil\n        (remove-if-not #'a^logic-formula-p (aux (formal-node-formal formal-node_x))))))\n\n;; > (mapcar #'print (absorb-sub-logic-node (make-formal-node :formal '(.to .l_.a (.neg .l_.b)))))\n;; ((.TO .L_.A (.NEG .L_.B)) .L_.A (.NEG .L_.B) .L_.B)\n\n(defun absorb-sub-logic-node-list (|K-assumptions|)\n  (mapcar\n   #'(lambda (formal)\n       (make-formal-node :logic-type 'l^temporary\n                         :vars       nil\n                         :formal     formal))\n   (remove-duplicates\n    (reduce #'safe-append (mapcar #'absorb-sub-logic-node |K-assumptions|) :initial-value nil)\n    ;;:test #'kp^equals?\n    :test #'equalp)))\n\n;; > (mapcar #'(lambda (x) (print (formal-node-formal x)))\n;;                      (absorb-sub-logic-node-list (list\n;;                                                  (make-formal-node :formal '(.to (.to .l_.c .l_.x) (.neg .l_.b)))\n;;                                                  (make-formal-node :formal '(.to .l_.a (.neg .l_.b))))))\n;; ((.TO (.TO .L_.C .L_.X) (.NEG .L_.B)) (.TO .L_.C .L_.X) .L_.C .L_.X\n;; (.TO .L_.A (.NEG .L_.B)) .L_.A (.NEG .L_.B) .L_.B)\n\n;;;;;;\n\n(defun upto-list (n &optional (start-m 1))\n  (loop for i from start-m upto n\n        collect i))\n\n\n\n(defun search-proof-aux (|K-axioms| |K-assumptions| |A|_previous Synthetos-Queue rules\n                         detect-goal k-number-current iter-rest sub-formal-nodes)\n  ;; |A| は証明の列であり、list構造であるとする。\n  ;; list構造としての |A| は A_k を car, A_{k-1} を cadr, A_1 を cdddd...dddr == last とする。\n  (tagbody\n   start-loop\n     (format t \"~%K-number-current: ~A, rest-iter: ~A~%\" k-number-current iter-rest)\n     (let*\n         ((|A_k|_maybe (car Synthetos-Queue))\n         (_dummy (when |A_k|_maybe\n                   (report-step k-number-current (length Synthetos-Queue) (formal-node-formal |A_k|_maybe))))\n          (|A_k|-is-kp^is-proof?\n                (kp^is-proof? |A_k|_maybe |K-axioms| |K-assumptions| |A|_previous rules))\n          (|A_k|_is-appeared?\n                (kp^some-equals? |A_k|_maybe |A|_previous)))\n       (cond\n         ((not |A_k|-is-kp^is-proof?)\n          (warn \"something wrong logic-node is given.: ~A~%~%\" |A_k|_maybe) ;; K-証明で、証明されていないAの混入。\n          (setq Synthetos-Queue (cdr Synthetos-Queue))\n          (go start-loop))\n         (t\n          (let* ((|A_k|\n                   (if |A_k|_is-appeared?\n                       |A_k|_maybe\n                       (formal-node-give-k-number |A_k|_maybe k-number-current)))\n                 (s_IC-list (syntheisis-add-immediate-consequence rules |A_k| |A|_previous))\n                 (s_axiom-generates (syntheisis-to-axioms |K-axioms| |A_k| sub-formal-nodes))\n                 \n                 (synthetos-queue-next\n                   (safe-append s_IC-list (cdr Synthetos-Queue) s_axiom-generates))\n                 \n                 (|A|-next (cons |A_k| |A|_previous)))\n            (cond ((funcall detect-goal |A_k| |A|_previous)\n                   (format t \"Reached Goal:~%\")\n                   (return-from search-proof-aux (values |A_k| |A|-next)))\n                  ((null synthetos-queue-next)\n                   (format t \"Queue is nil.~%\")\n                   (return-from search-proof-aux |A|_previous))\n                  ((identity |A_k|_is-appeared?)\n                   (setq Synthetos-Queue synthetos-queue-next)\n                   (go start-loop))\n                  ((>= 0 (- iter-rest 1))\n                   (format t \"Reached iter-limt.~%\")\n                   (return-from search-proof-aux nil))\n                  (t\n                   ;(format t \"New formal: ~A~%\" (formal-node-formal |A_k|))\n                   (setq |A|_previous |A|-next)\n                   (setq Synthetos-Queue synthetos-queue-next)\n                   (setq k-number-current (+ 1 k-number-current))\n                   (setq iter-rest (- iter-rest 1))\n                   (go start-loop)))\n            ))))))\n\n(defun search-proof (axioms assumptions proof-into &key (num-iter-max 10000)) ;; 公理 仮定 推論法則 終了条件(==A_vなど)\n  ;; todo: register inference rules it. also needs paser and etc ....\n  (let* ((|A|_list_1 assumptions) ;; (append axioms assumptions)) ;; todo: axioms is schema\n         (|K-length| (length |A|_list_1))\n         (|A|_start\n             (reverse (mapcar #'(lambda (A_i_dush number) (formal-node-give-k-number A_i_dush number))\n                              |A|_list_1\n                              (upto-list |K-length|))))\n         (sub-formals_t0\n           (absorb-sub-logic-node-list assumptions)))\n    (search-proof-aux\n     axioms assumptions\n     |A|_start\n     |A|_list_1 ;;(reverse |A|_1)\n     (list (cons #'inference-top_to_down-rule-i  #'kp^is-ic?_inference-rule1)) ;; todo.\n     #'(lambda (|A_k| |A|-next) (declare (ignore |A|-next))\n         ;;(kp^formula-equal-p proof-into |A_k|)\n         (kp^some-equals? proof-into (list |A_k|)))\n     (1+ |K-length|)\n     num-iter-max\n     sub-formals_t0)))\n\n\n\n#|\n(defun search-proof-aux (|K-axioms| |K-assumptions| |A|_previous Synthetos-Queue rules\n                         detect-goal k-number-current iter-rest)\n  ;; |A| は証明の列であり、list構造であるとする。\n  ;; list構造としての |A| は A_k を car, A_{k-1} を cadr, A_1 を cdddd...dddr == last とする。\n  (format t \"~%K-number-current: ~A,~%\" k-number-current)\n  (mapcar #'(lambda (a_n) (format t \" ~A\"  (formal-node-k-number a_n)))\n          |A|_previous)\n  (format t \"~%\")\n  (labels ((next-aux (next_|A| next-Synthetos-Queue next-k-number)\n             (search-proof-aux |K-axioms| |K-assumptions| next_|A| next-Synthetos-Queue rules\n                               detect-goal next-k-number (- iter-rest 1))))\n    (let*\n        ((|A_k|_maybe (car Synthetos-Queue))\n         (|A_k|-is-kp^is-proof?\n               (kp^is-proof? |A_k|_maybe |K-axioms| |K-assumptions| |A|_previous rules))\n         (|A_k|_is-appeared?\n               (kp^some-equals? |A_k|_maybe |A|_previous)))\n      (cond\n        ((not |A_k|-is-kp^is-proof?)\n         (warn \"something wrong logic-node is given.: ~A~%~%\" |A_k|_maybe) ;; K-証明で、証明されていないAの混入。\n         (next-aux |A|_previous (cdr Synthetos-Queue) k-number-current))\n        (t\n         (let* ((|A_k|\n                  (if |A_k|_is-appeared?\n                      |A_k|_maybe\n                      (formal-node-give-k-number |A_k|_maybe k-number-current)))\n                (syntheisis-list (syntheisis-add-immediate-consequence rules |A_k| |A|_previous))\n                (synthetos-queue-next (append (cdr Synthetos-Queue) syntheisis-list))\n                (|A|-next (cons |A_k| |A|_previous)))\n           (cond ((funcall detect-goal |A_k| |A|_previous)\n                  (format t \"Reached Goal:~%\")\n                  (values |A_k| |A|-next))\n                 ((null synthetos-queue-next)\n                  (format t \"Queue is nil.~%\")\n                  |A|_previous)\n                 ((identity |A_k|_is-appeared?)\n                  (next-aux |A|_previous synthetos-queue-next k-number-current))\n                 ((>= 0 (- iter-rest 1))\n                  (format t \"Reached iter-limt.~%\")\n                  nil)\n                 (t\n                  (next-aux |A|-next synthetos-queue-next (+ 1 k-number-current)))\n                 #|(t\n                 (format t \"something wrong.~%\")\n                 nil)\n                 |#\n                 )\n           ))))))\n\n(defun search-proof (axioms assumptions proof-into &key (num-iter-max 10000)) ;; 公理 仮定 推論法則 終了条件(==A_vなど)\n  ;; todo: register inference rules it. also needs paser and etc ....\n  (let* ((|A|_list_1 (append axioms assumptions))\n         (|K-length| (length |A|_list_1))\n         (|A|_start\n             (reverse (mapcar #'(lambda (A_i_dush number) (formal-node-give-k-number A_i_dush number))\n                              |A|_list_1\n                              (upto-list |K-length|)))))\n    (search-proof-aux\n     axioms assumptions\n     |A|_start\n     |A|_list_1 ;;(reverse |A|_1)\n     (list (cons #'inference-top_to_down-rule-i  #'kp^is-ic?_inference-rule1)) ;; todo.\n     #'(lambda (|A_k| |A|-next) (declare (ignore |A|-next))\n         ;;(kp^formula-equal-p proof-into |A_k|)\n         (kp^some-equals? proof-into (list |A_k|)))\n     (1+ |K-length|)\n     num-iter-max)))\n|#\n\n;;;;;;\n\n;; todo ? 名前の扱い。 変数名の分離や衝突などなどについて。\n(defun test-of-search-poof_1 ()\n  (let*\n      (;; Propositional Logic\n       (Gamma_of_II (list *a-II-1* *a-ii-2* *a-II-3*))\n       (assumptions\n         (list (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `(.to .l_.A .l_.B))\n               (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `(.to .l_.B .l_.C))))\n       (proof-into\n         (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                           :formal `(.to .l_.A .l_.C))))\n    ;; ref P14.\n    ;; .Gamma ++ {A .to B, B .to C} .vdash A .to C\n    (search-proof Gamma_of_II\n                  assumptions\n                  proof-into\n                  :num-iter-max 10000)))\n\n(defun test-of-search-poof_1/2-explode ()\n  (let*\n      (;; Propositional Logic\n       (Gamma_of_II (list *a-II-1* *a-ii-2* *a-II-3*))\n       (assumptions\n         (list (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `(.to .l_.A (.to .l_.B .l_.C)))))\n       (proof-into\n         (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                           :formal `(.to (.to .l_.A .l_.B) (.to .l_.A .l_.C)))))\n    ;; Todo: (命題論理の)公理はシューマーであるのに、シューマーへの具体的な論理式の割り当てができて居ない。\n    ;; 故にQueueが枯れて探索できなくなった。\n    #|(absorb-sub-logic-node-list assumptions))) ;; 仮定の部分論理式一覧|#\n    #| ;; 部分論理式による公理の一覧(T_start)\n    (syntheisis-to-axioms Gamma_of_II\n    (car assumptions)\n    (absorb-sub-logic-node-list assumptions))))|#\n    ;; 導けないこと (又は導ける場合には、証明システムがシューマーから仮定を生じることに対応していること。又は、K-証明でない証明法。)\n    (search-proof Gamma_of_II\n                  assumptions\n                  proof-into\n                  :num-iter-max 10000)))\n\n\n(defun test-of-search-poof_1/2 ()\n  (let*\n      (;; Propositional Logic\n       (Gamma_of_II (list *a-II-1* *a-ii-2* *a-II-3*))\n       (assumptions\n         (list (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `(.to .l_.A (.to .l_.B .l_.C)))\n               (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `.l_.A)))\n       (proof-into\n         (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                           :formal `(.to (.to .l_.A .l_.B) (.to .l_.A .l_.C)))))\n    ;; Todo: (命題論理の)公理はシューマーであるのに、シューマーへの具体的な論理式の割り当てができて居ない。\n    ;; 故にQueueが枯れて探索できなくなった。\n    #|(absorb-sub-logic-node-list assumptions))) ;; 仮定の部分論理式一覧|#\n    #| ;; 部分論理式による公理の一覧(T_start)\n    (syntheisis-to-axioms Gamma_of_II\n    (car assumptions)\n    (absorb-sub-logic-node-list assumptions))))|#\n    (search-proof Gamma_of_II\n                  assumptions\n                  proof-into\n                  :num-iter-max 10000)))\n\n\n(defun test-of-search-poof_1/8 ()\n  (let*\n      (;; Propositional Logic\n       (Gamma_of_II (list *a-II-1* *a-ii-2* *a-II-3*))\n       (assumptions\n         (list (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal `(.to .l_.A .l_.B))\n               (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                                 :formal '.l_.A)))\n       (proof-into\n         (make-formal-node :logic-type 'l^hypoaxioma :vars ()\n                           :formal `.l_.B)))\n    (search-proof Gamma_of_II\n                  assumptions\n                  proof-into\n                  :num-iter-max 10000)))\n\n\n;;;;;;\n\n#|\n(defun inference-top_to_down-rule-I (formula+_1 formula+_2)\n  ;; h{ A h, (.to A B) h} h.vdash B\n  ;; symbols beginning with h means symbols at hyper layer (when look from formal world).\n  (let* (;; formal-formula.\n         (ff_1 (identity formula+_1)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         (ff_2 (identity formula+_2)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         ;; super-layer formulas: outer formal-formula.\n         ;;   (sl_1 (......)) ;; todo\n         ;;   (sl_2 (......)) ;; todo\n         ;; forms\n         (ff_ands `(h^ands ,ff_1 ,ff_2))\n         (sl_addition '()) ;; todo\n         ;; templates\n         )\n    (declare (ignore sl_addition)) ;; todo: detection with additional conditions\n    ((lambda (_x) (if _x (list _x) ())) ;; [_x] or [] ;; one-formulas or none-formulas\n     (unpack-match (?A ?B)\n         (match-left '(?A ?B) '(h^ands ?A (.to ?A ?B)) ff_ands '()) ;; template for formal-formula also\n       (declare (ignore ?A))\n       ?B) ;; todo: this is only formal world. implement additional conditions in hyper layer\n     )))\n\n(defun inference-top_to_down-rule-II (formula+_1)\n  ;; todo: {A} .vdash .forall x A . where A は任意の論理式、xは任意の階数の任意の変数\n  '() ;; temporary, none-formulas\n  )\n|#\n\n\n\n#|\n\n結論から前提へと向かう推論に於いて、結論から前提へと至る木のフラクタルが証明を満たすとは、、、、\n前提となっているGから見て、{A1,A2,A3,...,An}が有るときに、\nG |- {A1,A2,A3,...,An} が見たされること。\n再帰的に上から下へと巡ってゆき、上記の制約が満たされる前提から小定理への推論が、\n結論に至るまで満たされる、その様な具体的な選択が一本以上、存在することを言う。\n\n\n□を書いて、その中に○をひとつかふたつ書く。それらのどれかひとつの□のうちの全ての○が\n1. 前提であるか\n2. 前段の□を満たすか\nのどちらかであるとき、その□と○の木構造の経路は、証明の一例である。\n\n\n○を仮定、□を仮定から導かれたそれぞれの仮定の集まり、△を□の集まりとする。\n○の前段に△などで層を描いて、△へと○から導ける仮定の種類の数だけ□を書いて、\nその中□のそれぞれの中に○をそれぞれ必要な数だけ書く。\n□の集まりで構成された或る層のうちに或るひとつ□の中の全ての○が\n1. 前提であるか\n2. 前段での層のうちに或るひとつの□の中の全ての○が成立すか、\nのどちらかであるとき、その□と○の木構造の経路は、証明の一例である。\n\n\n\n\n|#\n\n;;;; \\S 8.1 証明の形式化 より\n;; 直接の結論 (immediate consequence)\n;; -------begin almost Quote-------from [kiso_1] Section 8.2 -------\n;; (a). 推論規則1によると、論理式 A と論理式 A .to B から 論理式 B を導くことができる。\n;;      論理式 B は直接の結論である。\n;; (b). 推論規則2によると、論理式 A から 論理式 .forall .xi A を導くことができる。(ここで .xi は任意の階数の任意の変数である。)\n;;      論理式 .forall .xi A は直接の結論である。\n;; 直接の結論とは、上記の (a). , (b). の二種類以外には無い。\n;; -------end Quote------\n;;\n;; todo Future: (a). (b). 以外の推論規則にも対応できる様に、推論規則についても解釈器・実行器を作ること。\n\n\n#|\n(defun maybe-make-the-a_form-immediate-consequence (formula+_1 formula+_2)\n  ;; h{ A h, (.to A B) h} h.vdash B\n  ;; symbols beginning with h means symbols at hyper layer (when look from formal world).\n  (let* (;; formal-formula.\n         (ff_1 (identity formula+_1)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         (ff_2 (identity formula+_2)) ;; and also, skipping a^logic-formula-p in this implemation. as note.\n         ;; super-layer formulas: outer formal-formula.\n         ;;   (sl_1 (......)) ;; todo\n         ;;   (sl_2 (......)) ;; todo\n         ;; forms\n         (ff_ands `(h^ands ,ff_1 ,ff_2))\n         (sl_addition '()) ;; todo\n         ;; templates\n         )\n    (declare (ignore sl_addition)) ;; todo: detection with additional conditions\n    ((lambda (_x) (if _x (list _x) +fail+))\n     (unpack-match (?A ?B)\n         (match-left '(?A ?B) '(h^ands ?A (.to ?A ?B)) ff_ands '()) ;; template for formal-formula also\n       (declare (ignore ?A))\n       ?B) ;; todo: this is only formal world. implement additional conditions in hyper layer\n)))\n\n;; examples\n;; LENNMA-MATH> (maybe-make-the-a_form-immediate-consequence 'a '(.to x b))\n;; +FAIL+\n;; LENNMA-MATH> (maybe-make-the-a_form-immediate-consequence '(* 2 1) '(.to (* 2 1) b))\n;; B\n;; LENNMA-MATH> (maybe-make-the-a_form-immediate-consequence '(* 2 1) '(.to (* 2 1) (foo bar baz)))\n;; (FOO BAR BAZ)\n|#\n\n\n#|\n(mf^subst A x) が F(x)、\n(ma^subst A x t) が F(t) に展開されるテンプレート。\n(mf^ssforall x A) を論理式(.forall x A)を代表する論理式と定義して(?)、\n(ma^ssforall x t A) を具体的な (.forall t A) と展開されるテンプレートとする(?)。\n|#\n\n\n#|\n;; todo. A .vdash (.forall .xi A)\n(defun maybe-make-the-b_form-immediate-consequence (formula+_1)\n;;  ;; todo h{ A h} h.vdash (.forall .xi A)\n  t +fail+\n  )\n|#\n\n\n;;;;;; tests\n\n\n\n(defun test-of-of-kp^is-axiom? ()\n  (print (kp^is-axiom? (make-formal-node :logic-type 'l^hypothesis\n                                         :vars       '()\n                                         :formal     '(.to .l_.foo (.to .l_.bar .l_.foo .l_.baz)))\n                       *axiom-list-II*))\n  ;; > nil\n  (print (kp^is-axiom? (make-formal-node :logic-type 'l^hypothesis\n                                         :vars       '()\n                                         :formal     '(.to .l_.foo (.to .l_.bar .l_.foo)))\n                       *axiom-list-II*))\n  ;; > (list-of (:formal #S(FORMAL-NODE foobar... ) :bind (the_binds ...)) .... )\n  nil\n  )\n\n(defun test-of-of-kp^some-equals? ()\n  (let ((assumptions\n          (list (make-formal-node :logic-type 'l^axiom\n                                  :vars   '() ;; constance axiom (temporary)\n                                  :formal '(.to .l_.a .l_.b))\n                (make-formal-node :logic-type 'l^hypoaxioma\n                                  :formal '(.to .l_.c .l_.d))\n                (make-formal-node :logic-type 'l^lemma\n                                  :formal '(.to .l_.e .l_.d)))))\n    (print (kp^some-equals? (make-formal-node :logic-type 'l^hypothesis :formal '(.to .l_.c .l_.d)) assumptions)) ;; > (K_baz)\n    (print (kp^some-equals? (make-formal-node :logic-type 'l^hypothesis :formal '(.to .l_.a .l_.b)) assumptions)) ;; > (K_foo)\n    (print (kp^some-equals? (make-formal-node :logic-type 'l^hypothesis :formal '(.to .l_.e .l_.d)) assumptions)) ;; > nil\n    (print (kp^some-equals? (make-formal-node :logic-type 'l^hypothesis :formal '(.to .l_.a .l_.d)) assumptions)) ;; > nil\n    nil\n    ))\n\n(defun test-of-kp^is-ic?_inference-rule1 ()\n  (let ((IC-detectors\n          (list #'kp^is-ic?_inference-rule1)) ;; todo: #'kp^is-ic?_inference-rule2 , 述語論理.\n        (|As|\n          (list (make-formal-node :logic-type 'l^hypoaxioma\n                                  :vars   '() ;; constance axiom (temporary)\n                                  :formal '.l_.a)\n                (make-formal-node :logic-type 'l^lemma\n                                  :vars   '() ;; constance axiom (temporary)\n                                  :formal '(.to .l_.a .l_.b))\n                (make-formal-node :logic-type 'l^lemma\n                                  :vars   '() ;; constance axiom (temporary)\n                                  :formal '(.to (.to .l_.c .l_.d) (.to .l_.a .l_.b)))\n                (make-formal-node :logic-type 'l^lemma\n                                  :vars   '() ;; constance axiom (temporary)\n                                  :formal '(.to .l_.c .l_.d)))))\n    ;; avoid self matching\n    (print (unpack-match (?U)\n               (match-left '(?U) `(.to ?U a) `(.to a a) '())\n             (match-left '() ?U `(.to a a) '())))\n    ;; > +FAIL-UNIFY+\n    (print (unpack-match (?U)\n               (match-left '(?U) `(.to ?U a) `(.to a a) '())\n             (match-left '() ?U `(.to (.to a a) a) '())))\n    ;; > +FAIL-UNIFY+\n    ;; tests\n    (print (kp^is-ic? (make-formal-node :logic-type 'l^hypothesis\n                                        :vars '()\n                                        :formal '.l_.b)\n                      IC-detectors |As|)) ;; > ((h^ands ...) ....)\n    (print (kp^is-ic? (make-formal-node :logic-type 'l^hypothesis\n                                        :vars '()\n                                        :formal '.l_.z)\n                      IC-detectors |As|)) ;; > nil\n    (print (kp^is-ic? (make-formal-node :logic-type 'l^hypothesis\n                                        :vars '()\n                                        :formal '.l_.d)\n                      IC-detectors |As|)) ;; > nil\n    (print (kp^is-ic? (make-formal-node :logic-type 'l^hypothesis\n                                        :vars '()\n                                        :formal '(.to .l_.a .l_.b))\n                      IC-detectors |As|)) ;; > ((h^ands ...) ....)\n    nil\n    ))\n\n\n\n";
